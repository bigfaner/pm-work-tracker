---
status: "completed"
started: "2026-04-27 23:50"
completed: "2026-04-27 23:56"
time_spent: "~6m"
---

# Task Record: 1.1 Add isSoftDeletable[T]() and fix FindByID/FindByIDs

## Summary
Added isSoftDeletable[T]() runtime type switch helper to pkg/repo/helpers.go and updated FindByID[T] and FindByIDs[T] to automatically filter soft-deleted records for types that embed BaseModel (User, Team, MainItem, SubItem, ItemPool, Role, TeamMember). Non-soft-deletable types (ProgressRecord, StatusHistory) remain unaffected.

## Changes

### Files Created
无

### Files Modified
- backend/internal/pkg/repo/helpers.go
- backend/internal/pkg/repo/helpers_test.go

### Key Decisions
- Used inline .Where("deleted_flag = 0") instead of importing NotDeleted scope from repository/gorm package (cross-package import not possible)
- Used negative list in isSoftDeletable (only ProgressRecord and StatusHistory return false) so all new models with BaseModel are covered by default

## Test Results
- **Passed**: 24
- **Failed**: 0
- **Coverage**: 87.5%

## Acceptance Criteria
- [x] isSoftDeletable[T]() returns true for User, Team, MainItem, SubItem, ItemPool, Role, TeamMember
- [x] isSoftDeletable[T]() returns false for ProgressRecord, StatusHistory
- [x] FindByID[T] adds .Where(deleted_flag = 0) when isSoftDeletable[T]() is true
- [x] FindByIDs[T] adds .Where(deleted_flag = 0) when isSoftDeletable[T]() is true
- [x] Existing tests pass (no regression for ProgressRecord/StatusHistory callers)
- [x] New test: soft-deleted User record returns ErrNotFound via FindByID
- [x] New test: soft-deleted MainItem record returns ErrNotFound via FindByID
- [x] New test: ProgressRecord FindByID works without NotDeleted error

## Notes
无
