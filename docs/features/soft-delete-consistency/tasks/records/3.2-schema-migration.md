---
status: "completed"
started: "2026-04-28 01:01"
completed: "2026-04-28 01:02"
time_spent: "~1m"
---

# Task Record: 3.2 Fix pmw_sub_items unique index in schema files

## Summary
Updated uk_sub_items_main_code unique index in both MySQL and SQLite schema files to include deleted_flag and deleted_time columns, allowing re-creation of sub-items with the same item_code after soft-delete.

## Changes

### Files Created
无

### Files Modified
- backend/migrations/MySql-schema.sql
- backend/migrations/SQLite-schema.sql

### Key Decisions
- Added deleted_flag and deleted_time to uk_sub_items_main_code unique index, matching the pattern already used by uk_main_items_team_code_deleted
- For existing databases, users must run: ALTER TABLE pmw_sub_items DROP INDEX uk_sub_items_main_code, ADD UNIQUE KEY uk_sub_items_main_code (main_item_key, item_code, deleted_flag, deleted_time);

## Test Results
- **Passed**: 0
- **Failed**: 0
- **Coverage**: N/A (task has no tests)

## Acceptance Criteria
- [x] MySQL schema: UNIQUE KEY uk_sub_items_main_code (main_item_key, item_code, deleted_flag, deleted_time)
- [x] SQLite schema: CREATE UNIQUE INDEX IF NOT EXISTS uk_sub_items_main_code ON pmw_sub_items(main_item_key, item_code, deleted_flag, deleted_time);
- [x] Manual ALTER TABLE SQL documented for existing databases

## Notes
无
