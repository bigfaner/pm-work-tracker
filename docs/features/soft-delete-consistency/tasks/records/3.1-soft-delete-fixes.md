---
status: "completed"
started: "2026-04-28 00:54"
completed: "2026-04-28 01:00"
time_spent: "~6m"
---

# Task Record: 3.1 Fix SubItem SoftDelete and unify TeamMember RemoveMember

## Summary
Fix SubItem SoftDelete and TeamMember RemoveMember to use Updates with deleted_flag/deleted_time instead of hard delete. SubItem SoftDelete now sets deleted_flag=1 and deleted_time via Updates(). TeamMember RemoveMember now uses Updates() with a deleted_flag=0 WHERE clause. Updated TeamMember model to remove GORM uniqueIndex tags (real schema uses composite index uk_team_user_deleted). Updated test setup to create matching composite unique index.

## Changes

### Files Created
无

### Files Modified
- backend/internal/repository/gorm/sub_item_repo.go
- backend/internal/repository/gorm/team_repo.go
- backend/internal/repository/gorm/sub_item_repo_test.go
- backend/internal/repository/gorm/team_repo_test.go
- backend/internal/model/team.go

### Key Decisions
- SubItem SoftDelete is idempotent (RowsAffected==0 silently ignored), matching existing behavior
- RemoveMember adds AND deleted_flag=0 to WHERE clause to only soft-delete active members
- Removed GORM uniqueIndex tags from TeamMember model since the real schema uses a composite index (uk_team_user_deleted on team_key, user_key, deleted_flag, deleted_time) that can't be expressed via GORM struct tags on BaseModel fields
- Test setup creates composite unique index manually via raw SQL after AutoMigrate to match production schema

## Test Results
- **Passed**: 6
- **Failed**: 0
- **Coverage**: 74.4%

## Acceptance Criteria
- [x] SubItem SoftDelete uses Updates(map[string]any{"deleted_flag": 1, "deleted_time": time.Now()}) instead of db.Delete()
- [x] TeamMember RemoveMember uses Updates(map[string]any{"deleted_flag": 1, "deleted_time": time.Now()}) instead of db.Delete()
- [x] RemoveMember still returns ErrNotFound when RowsAffected == 0
- [x] New test: SubItem SoftDelete sets deleted_flag=1 and deleted_time
- [x] New test: SubItem re-create with same item_code after soft-delete succeeds
- [x] New test: TeamMember RemoveMember soft-deletes (record still in DB with deleted_flag=1)
- [x] New test: TeamMember re-add after RemoveMember succeeds (new row created)

## Notes
无
