---
status: "completed"
started: "2026-04-28 00:22"
completed: "2026-04-28 00:30"
time_spent: "~8m"
---

# Task Record: 2.2 Add NotDeleted to team_repo methods

## Summary
Added NotDeleted/NotDeletedTable scopes to 8 team_repo methods: List, ListFiltered, FindByBizKey, FindMember, ListMembers, CountMembers, FindPMMembers, FindTeamsByUserIDs. Added 4 new tests verifying soft-deleted teams and members are excluded from query results.

## Changes

### Files Created
无

### Files Modified
- backend/internal/repository/gorm/team_repo.go
- backend/internal/repository/gorm/team_repo_test.go

### Key Decisions
- Used .Scopes(NotDeleted) for single-table queries (List, ListFiltered, FindByBizKey, FindMember, CountMembers)
- Used .Scopes(NotDeletedTable("pmw_users")) for ListMembers and FindPMMembers which join pmw_users
- Used .Scopes(NotDeletedTable("pmw_teams")) for FindTeamsByUserIDs which joins pmw_teams
- Inserted scopes after .WithContext(ctx) and Joins, before .Where() — consistent with existing patterns

## Test Results
- **Passed**: 4
- **Failed**: 0
- **Coverage**: 13.8%

## Acceptance Criteria
- [x] List adds .Scopes(NotDeleted) — excludes soft-deleted teams
- [x] ListFiltered adds .Scopes(NotDeleted) — excludes from search
- [x] FindByBizKey adds .Scopes(NotDeleted) — returns error for soft-deleted team
- [x] ListMembers adds .Scopes(NotDeletedTable("pmw_users")) — excludes deleted users from member list
- [x] FindMember adds .Scopes(NotDeleted) on pmw_team_members — excludes soft-deleted members
- [x] CountMembers adds .Scopes(NotDeleted) on pmw_team_members — counts only active members
- [x] FindPMMembers adds .Scopes(NotDeletedTable("pmw_users")) — excludes deleted users
- [x] FindTeamsByUserIDs adds .Scopes(NotDeletedTable("pmw_teams")) — excludes deleted teams
- [x] New test: soft-deleted team not found by FindByBizKey
- [x] New test: soft-deleted team not in List
- [x] New test: soft-deleted member not found by FindMember
- [x] New test: CountMembers excludes soft-deleted members

## Notes
无
