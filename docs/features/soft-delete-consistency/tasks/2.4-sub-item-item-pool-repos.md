---
id: "2.4"
title: "Add NotDeleted to sub_item_repo and item_pool_repo"
priority: "P0"
estimated_time: "1h"
dependencies: ["1.gate"]
status: pending
breaking: false
---

# 2.4: Add NotDeleted to sub_item_repo and item_pool_repo

## Description

Add `.Scopes(NotDeleted)` to query methods in both `sub_item_repo.go` and `item_pool_repo.go`.

## Reference Files

- `backend/internal/repository/gorm/sub_item_repo.go` — 4 query methods (FindByBizKey, List, ListByMainItem, ListByTeam)
- `backend/internal/repository/gorm/item_pool_repo.go` — 2 methods (FindByBizKey, List)
- `docs/features/soft-delete-consistency/design/tech-design.md` — Modules 5 and 6

## Acceptance Criteria

- [ ] SubItem `FindByBizKey` adds `.Scopes(NotDeleted)` — returns error for soft-deleted sub-item
- [ ] SubItem `List` adds `.Scopes(NotDeleted)` — excludes soft-deleted from paginated list
- [ ] SubItem `ListByMainItem` adds `.Scopes(NotDeleted)` — excludes from main item's sub-items
- [ ] SubItem `ListByTeam` adds `.Scopes(NotDeleted)` — excludes from team sub-items
- [ ] ItemPool `FindByBizKey` adds `.Scopes(NotDeleted)` — returns error for soft-deleted pool item
- [ ] ItemPool `List` adds `.Scopes(NotDeleted)` — excludes from paginated list
- [ ] New test: soft-deleted sub-item not found by FindByBizKey
- [ ] New test: soft-deleted sub-item not in List results
- [ ] New test: soft-deleted item pool entry not found by FindByBizKey

## User Stories

- Story 2 AC: deleted sub-item disappears — SubItem List + FindByBizKey with NotDeleted

## Implementation Notes

SubItem repo also has `SoftDelete` — that's fixed in task 3.1 (separate concern). This task only adds NotDeleted to query methods.
