---
id: "5.1"
title: "Permission Handler Unit Tests"
priority: "P1"
estimated_time: "1-2h"
dependencies: ["4.gate"]
status: pending
---

# 5.1: Permission Handler Unit Tests

## Description

Write unit tests for `permission_handler.go` which currently has zero test coverage. Test `GetPermissions` and `GetPermissionCodes` handler methods using `httptest` + mock `RoleService` interface.

## Reference Files

- `docs/features/integration-test-coverage/design/tech-design.md` — F6 Unit Test Gap Strategy
- `docs/features/integration-test-coverage/prd/prd-spec.md` — F6 Unit Test Gaps table (first row)
- `backend/internal/handler/permission_handler.go` — handler implementation
- `backend/internal/handler/main_item_handler_test.go` — existing handler test pattern (httptest + mock service)
- `backend/internal/service/role_service.go` — RoleService interface

## Acceptance Criteria

- [ ] `permission_handler_test.go` created in `backend/internal/handler/`
- [ ] `GetPermissions` has at least one passing test (returns permissions list)
- [ ] `GetPermissionCodes` has at least one passing test (returns permission codes)
- [ ] All tests pass: `go test ./internal/handler/ -run TestPermission`

## User Stories

### Story 8: Unit Test Gaps (partial — permission_handler)

- **AC**: Given permission_handler.go has no tests, When tests are added, Then `GetPermissions` and `GetPermissionCodes` each have at least one passing test

## Implementation Notes

- Follow the handler test pattern from `main_item_handler_test.go`: httptest + Gin test context + mock service
- Create a `mockRoleService` struct implementing the `RoleService` interface with configurable function fields
- Test GetPermissions: mock returns list of permissions → verify 200 status and response body
- Test GetPermissionCodes: mock returns permission codes → verify 200 status and response body
