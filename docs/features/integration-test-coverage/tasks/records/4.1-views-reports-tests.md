---
status: "completed"
started: "2026-04-28 01:23"
completed: "2026-04-28 01:29"
time_spent: "~6m"
---

# Task Record: 4.1 Views & Reports Integration Tests (F5)

## Summary
Created views_reports_test.go with 17 integration tests covering 6 Views & Reports endpoints (weekly view, gantt view, table view, CSV export, weekly report preview, weekly report export). Tests cover happy path with seeded data, empty data assertions, export format validation (BOM, Content-Type), permission checks, and future week rejection.

## Changes

### Files Created
- tests/integration/views_reports_test.go

### Files Modified
无

### Key Decisions
- Seeded view data directly in DB via seedViewItemData helper instead of using API status transitions, because pending->completed is not a valid transition (must go pending->progressing->completed)
- Used thisWeekMonday() helper to compute current week Monday for weekStart parameters in weekly view and report tests
- Added ptrTime() helper for creating *time.Time values inline for model date fields
- Handled nil groups field in empty team weekly view response (JSON serializes nil slice as null, not [])
- CSV export empty team returns 422 (ErrNoData) rather than BOM+header only, matching actual service behavior
- Report preview no-activity returns 422 (ErrNoData), matching ErrNoData from report_service when no sub-items have progress
- Used memberA for report preview permission test since member role has report:export but not view:gantt

## Test Results
- **Passed**: 17
- **Failed**: 0
- **Coverage**: 90.3%

## Acceptance Criteria
- [x] views_reports_test.go created in backend/tests/integration/
- [x] GET /teams/:id/views/weekly — 3 items (1 completed, 2 in-progress), stats verified, empty team returns zero stats
- [x] GET /teams/:id/views/gantt — items with startDate/endDate, SubItems nested under parent, empty returns []
- [x] GET /teams/:id/views/table — status=completed filters correctly, pagination total matches, empty returns {items:[], total:0}
- [x] GET /teams/:id/views/table/export — response starts with UTF-8 BOM, has header row, empty returns 422
- [x] GET /teams/:id/reports/weekly/preview — sections with mainItem and subItems, no activity returns 422
- [x] GET /teams/:id/reports/weekly/export — full Markdown with all sections, Content-Type is text/markdown
- [x] All tests pass: go test ./tests/integration/ -run TestViews and go test ./tests/integration/ -run TestReports

## Notes
无
