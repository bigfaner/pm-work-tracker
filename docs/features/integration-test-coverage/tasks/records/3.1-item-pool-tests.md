---
status: "completed"
started: "2026-04-28 00:59"
completed: "2026-04-28 01:06"
time_spent: "~7m"
---

# Task Record: 3.1 Item Pool Integration Tests (F2)

## Summary
Created item_pool_test.go with 17 integration tests covering 6 Item Pool endpoints: submit (201, 400 on missing title, 403 for non-member), list with status filter (200), detail (200, 404), assign (200 with SubItem creation, rollback on invalid MainItem, 422 on already-processed, 403 for member), convert-to-main (200, 403, 422), reject (200, 400 on missing reason, 403, 422). Used json.Number decoder to avoid float64 precision loss on large BizKey values.

## Changes

### Files Created
- backend/tests/integration/item_pool_test.go

### Files Modified
无

### Key Decisions
- Used json.NewDecoder with UseNumber() for BizKey comparison to avoid float64 precision loss on large snowflake IDs
- Tested already-processed state mutual exclusion with 422 status (matching ErrItemAlreadyProcessed definition) rather than 409 from task AC
- Used setupTestDB+setupTestRouterWithDB pattern for tests needing DB verification, setupTestRouter for simpler endpoint tests
- Used userB (PM of teamB, not member of teamA) for 403 tests instead of memberA who has item_pool:submit permission

## Test Results
- **Passed**: 17
- **Failed**: 0
- **Coverage**: 39.3%

## Acceptance Criteria
- [x] item_pool_test.go created in backend/tests/integration/
- [x] POST /teams/:id/item-pool submit returns 201, missing title returns 400, non-member returns 403
- [x] GET /teams/:id/item-pool list with status filter returns 200
- [x] GET /teams/:id/item-pool/:poolId detail returns 200, not-found returns 404
- [x] POST /teams/:id/item-pool/:poolId/assign valid creates SubItem + updates pool 200, invalid MainItem rolls back, member 403, already-processed 422
- [x] POST /teams/:id/item-pool/:poolId/convert-to-main converts to MainItem 200, member 403, already-processed 422
- [x] POST /teams/:id/item-pool/:poolId/reject with reason 200, missing reason 400, member 403, already-processed 422
- [x] All tests pass: go test ./tests/integration/ -run TestItemPool

## Notes
无
