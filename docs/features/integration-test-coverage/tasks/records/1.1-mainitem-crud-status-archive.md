---
status: "completed"
started: "2026-04-28 00:19"
completed: "2026-04-28 00:28"
time_spent: "~9m"
---

# Task Record: 1.1 MainItem CRUD + Status + Archive Integration Tests

## Summary
Created item_lifecycle_test.go with 20 integration tests covering 7 MainItem endpoints: create (201, 400 on missing title, 403 for member), list (200, paginated), detail (200, 403 non-member, 404), update (200, 422 terminal state, 403 member, 404), change status (200 valid transition, 422 invalid, 403 member), available transitions (200 with list, 200 empty for terminal), archive (200 completed, 422 in-progress, 404). Used setupRBACTestDB/router with added main_item:change_status PM permission.

## Changes

### Files Created
- backend/tests/integration/item_lifecycle_test.go

### Files Modified
无

### Key Decisions
- Used setupRBACTestDB + setupRBACTestRouter from rbac_test.go instead of setupTestRouter from auth_isolation_test.go because RBAC test setup has more complete permission seeding
- Added main_item:change_status permission to PM role in setupLifecycleTest helper since the seed data (both auth_isolation and rbac) is missing this permission required by the router
- BizKey is a string in API responses (via pkg.FormatID), so createTestMainItem returns string and URLs use %s formatting

## Test Results
- **Passed**: 20
- **Failed**: 0
- **Coverage**: N/A (task has no tests)

## Acceptance Criteria
- [x] item_lifecycle_test.go created in backend/tests/integration/
- [x] POST /teams/:id/main-items — create returns 201, missing title returns 422(400), member returns 403
- [x] GET /teams/:id/main-items — paginated list returns 200
- [x] GET /teams/:id/main-items/:itemId — detail returns 200, non-member returns 403, not-found returns 404
- [x] PUT /teams/:id/main-items/:itemId — update returns 200, terminal state update returns 422, member returns 403, not-found returns 404
- [x] PUT /teams/:id/main-items/:itemId/status — valid transition returns 200, invalid transition returns 422, member returns 403
- [x] GET /teams/:id/main-items/:itemId/available-transitions — returns transition list, terminal returns empty
- [x] POST /teams/:id/main-items/:itemId/archive — completed item archives 200, in-progress returns 422, not-found returns 404
- [x] All tests pass: go test ./tests/integration/ -run TestItemLifecycle

## Notes
无
