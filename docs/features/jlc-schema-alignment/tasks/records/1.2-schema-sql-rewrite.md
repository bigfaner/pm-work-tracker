---
status: "completed"
started: "2026-04-25 18:18"
completed: "2026-04-25 18:20"
time_spent: "~2m"
---

# Task Record: 1.2 schema.sql MySQL 化重写

## Summary
将 backend/migrations/schema.sql 从 SQLite 语法完整重写为 MySQL 8.0 兼容 DDL。8 张表全部使用 pmw_ 前缀，新增 biz_key/create_time/db_update_time/deleted_flag/deleted_time 通用字段，TEXT→VARCHAR，REAL→DECIMAL(5,2)，BOOLEAN→TINYINT(1)，status 字段重命名，FK 字段 *_id→*_key，索引符合 idx_/uk_ 规范，无 FOREIGN KEY 约束，每张表有 COMMENT。

## Changes

### Files Created
无

### Files Modified
- backend/migrations/schema.sql

### Key Decisions
- 直接使用 tech-design §Data Models 中的完整 DDL，无需重新设计
- pmw_progress_records 和 pmw_status_histories 无 biz_key、无软删字段（append-only 表）

## Test Results
- **Passed**: 0
- **Failed**: 0
- **Coverage**: 0.0%

## Acceptance Criteria
- [x] schema.sql 包含 8 张表（pmw_ 前缀）
- [x] 所有表含 COMMENT
- [x] 所有索引符合 idx_/uk_ 命名规范
- [x] 无 TEXT 字段，无 FLOAT/DOUBLE/REAL，无 BOOLEAN
- [x] 无 FOREIGN KEY 约束
- [x] 无 status 直接用作字段名
- [x] pmw_progress_records 和 pmw_status_histories 无 biz_key、无软删字段

## Notes
无
