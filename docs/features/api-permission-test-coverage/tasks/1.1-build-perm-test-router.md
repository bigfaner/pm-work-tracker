---
id: "1.1"
title: "buildPermTestRouter helper + mock service stubs"
priority: "P0"
estimated_time: "1.5h"
dependencies: []
status: pending
breaking: false
---

# 1.1: buildPermTestRouter helper + mock service stubs

## Description

在 `backend/internal/handler/permission_matrix_test.go` 中实现 `buildPermTestRouter` 辅助函数，以及 6 个 mock service 类型（`mockMainItemSvc`、`mockTeamSvc`、`mockProgressSvc`、`mockItemPoolSvc`、`mockViewSvc`、`mockReportSvc`）。

这是 U1 所有 12 个 `TestPermMatrix_*` 函数的基础设施，必须先完成。

## Reference Files

- `design/tech-design.md` § Interfaces — `buildPermTestRouter` 签名 + mock 类型列表
- `backend/internal/middleware/permission.go` — `RequirePermission` 实现
- `backend/internal/handler/*_test.go` — 现有 mock 模式参考
- `backend/internal/handler/router.go` — handler 构造函数签名

## Acceptance Criteria

- [ ] `buildPermTestRouter(code string, permCodes []string, handlerFn gin.HandlerFunc) *gin.Engine` 实现完成
- [ ] 注入 middleware 正确设置 `permCodes` 和 `teamID`（uint(1)）到 context
- [ ] 6 个 mock service 类型实现，每个实现对应 handler 所需的最小接口方法（返回零值/nil error）
- [ ] `go build ./internal/handler/` 无编译错误
- [ ] 文件位于 `backend/internal/handler/permission_matrix_test.go`，package 为 `handler`

## User Stories

Story 1 (前置条件): 测试基础设施，无直接用户故事映射。

## Implementation Notes

- mock 方法只需返回零值，不需要 `mock.Mock` 断言（permission 测试不关心 handler 逻辑）
- `buildPermTestRouter` 注册路由为 `POST /test`，handler 返回 `c.Status(http.StatusOK)`（由 handlerFn 参数传入）
- `teamID` 必须注入，否则 handler 会 panic（`middleware.GetTeamID` 期望 context 中有值）
- 参考 `middleware/permission_test.go` 中的 `setupTeamPermRouter` 模式
