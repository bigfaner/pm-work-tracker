---
id: "4.5"
title: "Split MainItemDetailPage.tsx"
priority: "P0"
estimated_time: "3h"
dependencies: ["3.6"]
status: pending
---

# 4.5: Split MainItemDetailPage.tsx

## Description

Decompose `MainItemDetailPage.tsx` (927 lines) into ~5 focused files, each ≤300 lines. Extract inline dialogs into separate files.

## Reference Files

- `frontend/src/pages/MainItemDetailPage.tsx` — source (927 lines)
- Target files:
  - `MainItemDetailPage.tsx` — main page component (~200 lines)
  - `EditMainItemDialog.tsx` — dialog (~100 lines)
  - `CreateSubItemDialog.tsx` — dialog (~100 lines)
  - `EditSubItemDialog.tsx` — dialog (~100 lines)
  - `AppendProgressDialog.tsx` — dialog (~100 lines)

## Acceptance Criteria

- [ ] No page file exceeds 300 lines
- [ ] Each extracted file has a single responsibility
- [ ] `StatusTransitionDropdown` used instead of inline dropdowns (should already be done in 3.6)
- [ ] `npx tsc --noEmit` passes
- [ ] `npm test` passes

## Implementation Notes

Same approach as 4.4. Extract dialogs first, then verify the main page component is under 300 lines. If MainItemDetailPage and ItemViewPage share dialog components (e.g., `CreateSubItemDialog`), consider placing them in `components/shared/` or a shared directory. But don't over-abstract — only share if the dialog is truly identical.
