---
id: "1.1"
title: "Backend dead code removal"
priority: "P0"
estimated_time: "2h"
dependencies: []
status: pending
---

# 1.1: Backend Dead Code Removal

## Description

Delete all unused backend functions, types, files, and dead assignments identified in the PRD Phase 1 table.

## Reference Files

- `backend/internal/handler/sub_item_handler.go` — delete `parseItemIDAsUint`
- `backend/internal/handler/admin_handler.go` — delete `paginateUsers`
- `backend/internal/service/admin_service.go` — delete `derefStr`
- `backend/internal/pkg/report/renderer.go` — delete `Filename`
- `backend/internal/vo/team_vo.go` — delete entire file
- `backend/internal/middleware/rbac.go` — delete entire file
- `backend/internal/middleware/team_scope.go` — delete `GetCallerTeamRole`
- `backend/internal/middleware/auth.go` — delete `GetUsername`
- `backend/internal/service/view_service.go` — delete `WeeklyView` method
- `backend/internal/service/role_service.go` — delete `codeMap` assignment

## Acceptance Criteria

- [ ] All listed functions, types, files are deleted
- [ ] `go vet ./...` passes with zero warnings
- [ ] `go test ./...` passes — all existing tests green
- [ ] No dangling imports referencing deleted items

## Implementation Notes

Delete items in dependency order: files first (rbac.go, team_vo.go), then functions in remaining files. After each deletion, check for compilation errors to catch unexpected dependencies early. Tests that reference deleted items (e.g., `rbac_test.go` if it exists) should also be removed.
