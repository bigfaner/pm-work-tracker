---
id: "4.4"
title: "Split ItemViewPage.tsx"
priority: "P0"
estimated_time: "3h"
dependencies: ["3.6"]
status: pending
---

# 4.4: Split ItemViewPage.tsx

## Description

Decompose `ItemViewPage.tsx` (1459 lines) into ~8 focused files, each ≤300 lines. Extract inline dialogs and sub-views into separate files.

## Reference Files

- `frontend/src/pages/ItemViewPage.tsx` — source (1459 lines)
- Target files:
  - `ItemViewPage.tsx` — main page component (~150 lines)
  - `ItemSummaryView.tsx` — summary sub-view (~180 lines)
  - `ItemDetailView.tsx` — detail sub-view (~140 lines)
  - `CreateMainItemDialog.tsx` — dialog (~100 lines)
  - `EditMainItemDialog.tsx` — dialog (~100 lines)
  - `CreateSubItemDialog.tsx` — dialog (~100 lines)
  - `EditSubItemDialog.tsx` — dialog (~100 lines)
  - `AppendProgressDialog.tsx` — dialog (~100 lines)

## Acceptance Criteria

- [ ] No page file exceeds 300 lines
- [ ] Each extracted file has a single responsibility
- [ ] `StatusTransitionDropdown` used instead of inline dropdowns (should already be done in 3.6)
- [ ] `npx tsc --noEmit` passes — all imports resolve
- [ ] `npm test` passes — no regressions

## Implementation Notes

Extract one component at a time. Start with the dialogs (simplest, self-contained). Then extract SummaryView and DetailView. The main `ItemViewPage` becomes a thin orchestrator that composes the sub-views and dialogs. Move files to a `pages/item-view/` directory if needed, or keep as siblings. Ensure React Query hooks move with their owning component.
