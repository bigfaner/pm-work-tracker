---
id: "1.3"
title: "Backend contract fix: SubItemVO +statusName"
priority: "P0"
estimated_time: "1h"
dependencies: ["1.1"]
status: pending
---

# 1.3: Backend Contract Fix — SubItemVO +statusName

## Description

Add `statusName` field to `SubItemVO` to match `MainItemVO` parity. Update the VO constructor to populate the field using the status-to-name mapping.

## Reference Files

- `backend/internal/vo/item_vo.go` — `SubItemVO` struct + `NewSubItemVO` constructor
- `backend/internal/pkg/status/status.go` — status name resolution function

## Acceptance Criteria

- [ ] `SubItemVO` struct has `StatusName string \`json:"statusName"\``
- [ ] `NewSubItemVO` constructor populates `StatusName` from status code
- [ ] `go test ./...` passes
- [ ] API response for sub-item endpoints now includes `statusName` field

## Implementation Notes

Check how `MainItemVO` populates its `StatusName` field — follow the same pattern. The status package likely has a `StatusToName` or similar function. If not, add a simple map lookup.
