---
id: "1.2"
title: "Frontend dead code removal"
priority: "P0"
estimated_time: "2h"
dependencies: []
status: pending
---

# 1.2: Frontend Dead Code Removal

## Description

Delete all unused frontend components, types, API functions, and exports identified in the PRD Phase 1 table.

## Reference Files

- `frontend/src/components/AdminRoute.tsx` — delete entire file
- `frontend/src/pages/AdminPage.tsx` — delete entire file
- `frontend/src/components/shared/ItemFilters.tsx` — delete file + test
- `frontend/src/api/mainItems.ts` — delete `archiveMainItemApi`
- `frontend/src/api/subItems.ts` — delete `assignSubItemApi`
- `frontend/src/api/itemPool.ts` — delete `getItemPoolApi`
- `frontend/src/api/progress.ts` — delete `correctCompletionApi`
- `frontend/src/api/client.ts` — remove `ApiSuccessEnvelope`, `ApiErrorEnvelope` exports
- `frontend/src/types/index.ts` — delete `WeeklyViewResp`, `WeeklyGroup`, `SubItemWithProgress`, `TeamMember`

## Acceptance Criteria

- [ ] All listed files, functions, types, exports deleted
- [ ] `npx tsc --noEmit` passes — no type errors
- [ ] `npm test` passes — all existing tests green
- [ ] No dangling imports referencing deleted items

## Implementation Notes

Delete files first, then remove functions/exports from remaining files. Check `modules.test.ts` for test-only references to deleted API functions — update or remove those test cases. After deletions, run `npx tsc --noEmit` to catch any remaining references.
