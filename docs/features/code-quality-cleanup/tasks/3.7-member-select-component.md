---
id: "3.7"
title: "Create MemberSelect component"
priority: "P2"
estimated_time: "2h"
dependencies: ["3.5"]
status: pending
---

# 3.7: Create MemberSelect Component

## Description

Extract the repeated member selection UI across 8+ dialogs into a shared `MemberSelect` component.

## Reference Files

- `frontend/src/components/shared/MemberSelect.tsx` — new file
- `frontend/src/pages/ItemViewPage.tsx` — inline member select in dialogs
- `frontend/src/pages/MainItemDetailPage.tsx` — inline member select in dialogs
- `frontend/src/pages/SubItemDetailPage.tsx` — inline member select in dialogs

## Acceptance Criteria

- [ ] `MemberSelect` component with props: `members`, `selectedId`, `onSelect`, `placeholder?`
- [ ] Renders a searchable select dropdown of team members
- [ ] Replaces inline member select in all dialog components
- [ ] `npm test` passes

## Implementation Notes

Check existing UI primitives in `components/ui/` — there may already be a `Select` or `Combobox` component to build on. The `MemberSelect` should use `useMemberName` internally for display name resolution. This is P2 because it's a nice-to-have dedup that doesn't affect performance or correctness.
