---
id: "4.1"
title: "Decompose WeeklyComparison"
priority: "P1"
estimated_time: "2h"
dependencies: ["3.3"]
status: pending
---

# 4.1: Decompose WeeklyComparison

## Description

Break the 288-line `WeeklyComparison` method into smaller, focused functions for readability.

## Reference Files

- `backend/internal/service/view_service.go` — `WeeklyComparison` (line ~239)

## Acceptance Criteria

- [ ] `WeeklyComparison` method body is ≤50 lines, delegating to extracted helpers
- [ ] Each extracted function has a single responsibility (e.g., build comparison pairs, compute deltas, resolve names)
- [ ] `go test ./...` passes — identical behavior

## Implementation Notes

Candidate extractions: `buildComparisonPairs(current, previous)`, `computeWeeklyDeltas(pairs)`, `buildWeeklyResponse(pairs, deltas)`. The method currently mixes data fetching, computation, and response building — separate these concerns.
