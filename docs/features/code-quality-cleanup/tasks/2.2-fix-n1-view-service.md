---
id: "2.2"
title: "Fix N+1 in view_service"
priority: "P0"
estimated_time: "3h"
dependencies: ["2.1"]
status: pending
---

# 2.2: Fix N+1 in view_service

## Description

Replace per-ID `userRepo.FindByID` loops in `resolveAssigneeNames` and `WeeklyComparison` with batch `FindByIDs` calls. Fix `TableView` to use batch name resolution while keeping Go-level cross-entity filtering (hybrid strategy).

## Reference Files

- `backend/internal/service/view_service.go` — `resolveAssigneeNames` (line ~871), `WeeklyComparison` (line ~239), `TableView` (line ~573)

## Acceptance Criteria

- [ ] `resolveAssigneeNames` uses single `FindByIDs` call instead of per-ID loop
- [ ] `WeeklyComparison` uses single `FindByIDs` call for assignee names
- [ ] `TableView` uses batch name resolution (1 query for all assignees)
- [ ] `TableExportCSV` uses same batch pattern as `TableView`
- [ ] All methods produce identical output to current implementation
- [ ] `go test ./...` passes

## Implementation Notes

Collect all unique assignee IDs first, then call `FindByIDs` once, then iterate the map for name resolution. The cross-entity filtering logic in `TableView` stays in Go — only the name resolution changes from N queries to 1 query.
