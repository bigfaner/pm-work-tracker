---
id: "4.2"
title: "Re-run e2e tests to verify DecisionTimeline UI integration"
priority: P0
estimatedTime: "30min"
dependencies:
  - "4.1"
status: pending
scope: all
breaking: true
---

# Task 4.2: Re-run e2e tests to verify DecisionTimeline UI integration

## Context

After integrating DecisionTimeline and DecisionFormDialog into MainItemDetailPage (task 4.1), re-run the decision-log e2e tests to verify:
- TC-005 (previously failing: published decision content not visible) now passes
- Previously skipped UI tests (9 tests dependent on DecisionTimeline being in the page) can now be evaluated

## Requirements

1. Run `just test-e2e decision-log`
2. Analyze results:
   - TC-005 should now pass (DecisionTimeline renders on the detail page)
   - Previously skipped tests that depend on DecisionFormDialog may still skip if the form UI interactions aren't fully implemented
3. Update `tests/e2e/items/results/latest.md` with new results (tests moved from tests/e2e/decision-log/)
4. Record results in task record

## Acceptance Criteria

- [ ] `just test-e2e decision-log` completes without infrastructure errors
- [ ] TC-005 passes (published decision content visible on detail page)
- [ ] `just test backend` still passes (no backend regression)
- [ ] Results recorded in `tests/e2e/items/results/latest.md` (tests moved from tests/e2e/decision-log/)
