---
status: "completed"
started: "2026-04-30 16:48"
completed: "2026-04-30 16:52"
time_spent: "~4m"
---

# Task Record: 1.2 Implement update-package-json.ts

## Summary
Implemented update-package-json.ts which reads tests/e2e/package.json, merges new spec paths into test:api, test:ui, and test:cli scripts with deduplication, adds test:ui script and updates test script when UI specs are provided, and throws ERR_PACKAGE_JSON_WRITE on write failure. Added 10 unit tests covering all acceptance criteria.

## Changes

### Files Created
- docs/features/e2e-test-scripts-rebuild/testing/scripts/update-package-json.ts
- docs/features/e2e-test-scripts-rebuild/testing/scripts/update-package-json.test.ts

### Files Modified
无

### Key Decisions
- Used pkgPath parameter with default pointing to tests/e2e/package.json to allow test injection without touching real file
- Parsed existing script strings by splitting on ' && ' and filtering by NODE_TEST_PREFIX to extract spec paths
- Used Set-based deduplication to prevent duplicate paths
- Appended newline after JSON.stringify to match existing file formatting

## Test Results
- **Passed**: 10
- **Failed**: 0
- **Coverage**: N/A (task has no tests)

## Acceptance Criteria
- [x] updatePackageJson(specPaths: SpecPaths): void implemented with correct SpecPaths type
- [x] Reads current tests/e2e/package.json, merges new paths into test:api, test:ui, test:cli scripts
- [x] Adds test:ui script if it doesn't exist and specPaths.ui is non-empty
- [x] Updates test script to include npm run test:ui if UI specs added
- [x] Throws Error with code ERR_PACKAGE_JSON_WRITE on write failure
- [x] Does not duplicate paths already present in existing scripts
- [x] Unit tests pass: merge, dedup, and write-failure scenarios

## Notes
无
