---
status: "completed"
started: "2026-04-30 16:41"
completed: "2026-04-30 16:47"
time_spent: "~6m"
---

# Task Record: 1.1 Implement validate-spec.ts

## Summary
Implemented validate-spec.ts at docs/features/e2e-test-scripts-rebuild/testing/scripts/validate-spec.ts. The validator uses fs.readFileSync + regex to detect EXTERNAL_IMPORT (mocha/jest/chai/jasmine/vitest imports and bare describe()/it() calls without node:test), MISSING_TRACEABILITY (test() blocks without a '// Traceability: TC-' comment in the preceding 3 lines), and STALE_IMPORT_PATH (imports from testing/scripts/ paths). Returns ValidationResult with valid flag and typed ValidationError array.

## Changes

### Files Created
- docs/features/e2e-test-scripts-rebuild/testing/scripts/validate-spec.ts
- docs/features/e2e-test-scripts-rebuild/testing/scripts/validate-spec.test.ts
- docs/features/e2e-test-scripts-rebuild/testing/scripts/package.json

### Files Modified
无

### Key Decisions
- Used regex scan (no AST) as specified in task notes — sufficient for the three check types
- Added package.json with type:module in testing/scripts/ to enable ESM imports for node:test runner
- bare describe()/it() detection is suppressed when file imports from node:test, avoiding false positives on conformant specs

## Test Results
- **Passed**: 14
- **Failed**: 0
- **Coverage**: N/A (task has no tests)

## Acceptance Criteria
- [x] validateSpec(filePath: string): ValidationResult implemented with correct return types
- [x] Detects EXTERNAL_IMPORT: any import from mocha, jest, chai, jasmine, vitest, describe, it (external test frameworks)
- [x] Detects MISSING_TRACEABILITY: test() blocks without // Traceability: TC- comment
- [x] Detects STALE_IMPORT_PATH: imports containing testing/scripts/
- [x] Returns { valid: true, errors: [] } for a conformant spec
- [x] Unit tests pass: at least one test per error code + one happy-path test

## Notes
无
