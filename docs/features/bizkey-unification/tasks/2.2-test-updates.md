---
id: "2.2"
title: "Test updates: handler tests + service tests + integration tests"
priority: "P0"
estimated_time: "2.5h"
dependencies: ["1.gate"]
status: pending
breaking: false
---

# 2.2: Test updates: handler tests + service tests + integration tests

## Description

Update all test files to match the new `int64` signatures. Three categories:

**Handler tests (10 files)**: Update mock service interfaces and `GetTeamBizKey` context setup. Replace `c.Set("teamID", uint(X))` with `c.Set("teamBizKey", int64(X))`.

**Service tests (7 files)**: Already partially updated in Phase 1 tasks. Verify no remaining `uint` mock params for team-scoped methods.

**Integration tests**: Update `views_reports_test.go`, `helpers.go`, and any integration test that passes `teamID uint` to service calls.

## Reference Files

- `backend/internal/handler/*_test.go` (10 files)
- `backend/internal/service/*_test.go` (7 files — verify phase 1 updates are complete)
- `backend/tests/integration/views_reports_test.go`
- `backend/tests/integration/helpers.go`
- `design/tech-design.md` — Testing Strategy section

## Acceptance Criteria

- [ ] `grep -rn "c\.Set(\"teamID\"" backend/internal/handler/` returns zero results
- [ ] All handler test files set `c.Set("teamBizKey", int64(X))` in test context setup
- [ ] `go test -race ./internal/handler/...` passes
- [ ] `go test -race ./internal/service/...` passes
- [ ] `go test -race ./tests/integration/...` passes
- [ ] Coverage ≥ 75% on `internal/service` and `internal/handler` packages

## User Stories

**Story 1: 进度记录写入正确的 team_key**
- Given 用户属于 bizKey 为 `123456789012345678` 的团队
- When 用户提交一条进度记录
- Then 数据库中该记录的 `team_key` 字段值为 `123456789012345678`

## Implementation Notes

- Handler tests use `gin.CreateTestContext` + `c.Set(...)` to inject context values — update the key name and type
- Integration test helpers that build team context may use `GetTeamID` — update to `GetTeamBizKey`
- Run `go test -race -coverprofile=coverage.out ./internal/...` to verify coverage threshold
