---
id: "2.1"
title: "Handler updates: GetTeamID → GetTeamBizKey (all 7 handlers)"
priority: "P0"
estimated_time: "2h"
dependencies: ["1.gate"]
status: pending
breaking: false
---

# 2.1: Handler updates: GetTeamID → GetTeamBizKey (all 7 handlers)

## Description

Mechanical call-site updates across all 7 handler files. Replace `middleware.GetTeamID(c)` with `middleware.GetTeamBizKey(c)` and update service call arguments to pass `teamBizKey int64`.

Files to update:
- `handler/progress_handler.go`
- `handler/main_item_handler.go`
- `handler/sub_item_handler.go`
- `handler/item_pool_handler.go`
- `handler/view_handler.go`
- `handler/report_handler.go`
- `handler/team_handler.go` — additionally: pass `roleBizKey int64` to `UpdateMemberRole` and `InviteMember`

## Reference Files

- `design/tech-design.md` §7 (Handler call sites, Complete change surface table)
- `backend/internal/middleware/team_scope.go` — `GetTeamBizKey` (added in task 1.1)
- All 7 handler source files listed above

## Acceptance Criteria

- [ ] `grep -rn "GetTeamID" backend/internal/handler/` returns zero results
- [ ] All 7 handler files compile with `GetTeamBizKey`
- [ ] `team_handler.go`: `UpdateMemberRole` and `InviteMember` pass `roleBizKey int64` from parsed request body
- [ ] `go build ./...` passes with zero errors

## User Stories

**Story 3: 后端开发者新增功能时类型系统提供约束**
- Given Service 接口方法签名中，来自外部输入的 ID 参数类型为 `int64`
- When 开发者尝试将 `uint` 类型的内部 ID 直接传入该参数
- Then 编译器报类型不匹配错误，构建失败

## Implementation Notes

- This task depends on `1.gate` — all service interfaces must be updated before handlers can compile
- Changes are purely mechanical: rename the context accessor, update the variable name, pass it through
- `team_handler.go` role handling: the request body already contains `roleKey` as a string; parse it with `pkg.ParseID` to get `int64` and pass directly to service
