---
id: "1.4"
title: "ViewService + ReportService: teamID uint → teamBizKey int64"
priority: "P0"
estimated_time: "1.5h"
dependencies: []
status: pending
breaking: true
---

# 1.4: ViewService + ReportService: teamID uint → teamBizKey int64

## Description

Update `ViewService` and `ReportService` interfaces and implementations:

**ViewService** (`view_service.go`):
- `WeeklyComparison(ctx, teamID uint, ...)` → `(ctx, teamBizKey int64, ...)`
- `GanttView(ctx, teamID uint, ...)` → `(ctx, teamBizKey int64, ...)`
- `TableView(ctx, teamID uint, ...)` → `(ctx, teamBizKey int64, ...)`
- `TableExportCSV(ctx, teamID uint, ...)` → `(ctx, teamBizKey int64, ...)`

**ReportService** (`report_service.go`):
- `Preview(ctx, teamID uint, ...)` → `(ctx, teamBizKey int64, ...)`
- `ExportMarkdown(ctx, teamID uint, ...)` → `(ctx, teamBizKey int64, ...)`

## Reference Files

- `backend/internal/service/view_service.go` — source file to modify
- `backend/internal/service/view_service_test.go` — test file to update
- `backend/internal/service/report_service.go` — source file to modify
- `backend/internal/service/report_service_test.go` — test file to update
- `design/tech-design.md` §4-5 (ViewService, ReportService) — exact before/after signatures

## Acceptance Criteria

- [ ] `ViewService` interface: all 4 methods use `teamBizKey int64`
- [ ] `ReportService` interface: both methods use `teamBizKey int64`
- [ ] Implementations updated to pass `teamBizKey` to repo calls (repo takes `uint` — cast at repo call site)
- [ ] `view_service_test.go` and `report_service_test.go` mocks updated to `int64` params
- [ ] `go build ./internal/service/...` passes
- [ ] `go test -race ./internal/service/ -run "TestView|TestReport"` passes

## User Stories

No direct user story mapping — this is a mechanical interface alignment task.

## Implementation Notes

- Repo calls like `mainItemRepo.ListNonArchivedByTeam(ctx, teamID)` take `uint` — cast `uint(teamBizKey)` at those call sites; this is a known repo-layer limitation out of scope for this feature
- View service tests use `uint` values in mock setup — update to `int64`
