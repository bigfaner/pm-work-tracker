---
id: "3.1"
title: "Backend Unit Tests: buildWeeklyGroups 7 卡片计数"
priority: "P0"
estimated_time: "2h"
dependencies: "2.1"
status: pending
---

# 3.1: Backend Unit Tests: buildWeeklyGroups 7 卡片计数

## Description

在 `backend/internal/service/view_service_test.go` 中新增 `TestBuildWeeklyGroups_Stats`，使用 table-driven 结构覆盖 7 个卡片的计数规则。

## Reference Files

- `backend/internal/service/view_service_test.go`
- `backend/internal/service/view_service.go` (buildWeeklyGroups)
- `docs/features/weekly-stats-optimization/design/tech-design.md` (Interface 3)

## Acceptance Criteria

- [ ] 7 种状态各 1 个活跃子事项 → 7 个卡片计数均为 1
- [ ] pending 子事项不活跃 → pending 计数为 0
- [ ] overdue + progressing 同一事项 → overdue=1, inProgress=1（重叠）
- [ ] justCompleted 事项 → newlyCompleted=1, activeSubItems=1
- [ ] expectedEndDate=nil → overdue=0
- [ ] status=completed 且 expectedEndDate < weekEnd → overdue=0
- [ ] status=closed 且 expectedEndDate < weekEnd → overdue=0
- [ ] **条件优先级 fixture**：progressRecord ∈ week AND actualEndDate < weekStart → activeSubItems=1（进展记录优先于日期范围）
- [ ] `go test ./internal/service/...` 全部通过
- [ ] `buildWeeklyGroups` 行覆盖率 ≥ 90%

## Implementation Notes

使用 `go test` + `testify/assert`。fixture 使用最小化 model 数据，mock 进展记录 map 和活跃 map。
