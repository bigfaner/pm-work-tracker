---
id: "4.1"
title: "Frontend Unit Tests: StatsBar + isOverdue"
priority: "P1"
estimated_time: "2h"
dependencies: "3.2, 2.2"
status: pending
---

# 4.1: Frontend Unit Tests: StatsBar + isOverdue

## Description

新增 `WeeklyViewPage.test.tsx` 中 StatsBar 相关测试，并更新 `status.test.ts` 中 `isOverdue` 测试用例以补充 `referenceDate` 参数。

## Reference Files

- `frontend/src/pages/WeeklyViewPage.test.tsx`
- `frontend/src/lib/status.test.ts`
- `docs/features/weekly-stats-optimization/design/tech-design.md` (Interface 7)

## Acceptance Criteria

**WeeklyViewPage.test.tsx（新增）：**
- [ ] 7 个卡片数字正确渲染（`data-testid` 断言）
- [ ] hover tooltip 内容正确（`userEvent.hover`）
- [ ] 点击 tooltip 展开/收起（`userEvent.click`）
- [ ] Tab 聚焦后 `aria-describedby` 属性存在
- [ ] `isError=true` 时所有卡片显示 `"-"`

**status.test.ts（更新）：**
- [ ] 所有现有 `isOverdue` 测试用例补充 `referenceDate` 参数（传入固定日期，不依赖 `new Date()`）
- [ ] 新增：`referenceDate` 为过去时间，expectedEndDate 在其之后 → false

**通过标准：**
- [ ] `npx vitest run` 全部通过，无跳过

## Implementation Notes

使用 `vitest` + `@testing-library/react` + `userEvent`。status.test.ts 中现有测试传入 `new Date('2099-01-01')` 作为 referenceDate 以保持语义不变。
