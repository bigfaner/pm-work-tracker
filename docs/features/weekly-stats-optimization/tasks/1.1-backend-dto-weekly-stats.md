---
id: "1.1"
title: "Backend DTO: WeeklyStats 新增 3 字段"
priority: "P0"
estimated_time: "0.5h"
dependencies: ""
status: pending
---

# 1.1: Backend DTO: WeeklyStats 新增 3 字段

## Description

在 `backend/internal/dto/item_dto.go` 的 `WeeklyStats` struct 中新增 `Pending`、`Pausing`、`Overdue` 三个字段。

## Reference Files

- `backend/internal/dto/item_dto.go` (WeeklyStats struct, line ~149)

## Acceptance Criteria

- [ ] `WeeklyStats` 新增 `Pending int \`json:"pending"\``
- [ ] `WeeklyStats` 新增 `Pausing int \`json:"pausing"\``
- [ ] `WeeklyStats` 新增 `Overdue int \`json:"overdue"\``
- [ ] JSON tag 使用 camelCase（tagliatelle linter 通过）
- [ ] `go build ./...` 无编译错误

## Implementation Notes

Go struct 新增字段默认零值，JSON 序列化向后兼容。无需数据库变更。
