---
id: "4.1"
title: "Table rename to pmw_ prefix"
priority: "P1"
estimated_time: "1h"
dependencies: ["3.gate"]
status: pending
breaking: true
---

# 4.1: Table rename to pmw_ prefix

## Description

Rename `roles` → `pmw_roles` and `role_permissions` → `pmw_role_permissions` in model TableName methods and both schema files.

## Reference Files

- `backend/internal/model/role.go` — TableName methods
- `backend/migrations/SQLite-schema.sql` — DDL
- `backend/migrations/MySql-schema.sql` — DDL
- `backend/internal/model/role_test.go` — test assertions
- `docs/features/schema-alignment-cleanup/design/tech-design.md` — Model 3

## Acceptance Criteria

- [ ] `Role.TableName()` returns `"pmw_roles"`
- [ ] `RolePermission.TableName()` returns `"pmw_role_permissions"`
- [ ] Both SQLite and MySQL schema files contain `pmw_roles` and `pmw_role_permissions`
- [ ] `go test ./internal/model/ -run TestRole` passes

## User Stories

**Story 5: 表命名规范统一**
- Given `roles` 和 `role_permissions` 表缺少 `pmw_` 前缀
- When 重命名完成
- Then `Role.TableName()` 返回 `"pmw_roles"`
- And `RolePermission.TableName()` 返回 `"pmw_role_permissions"`
- And `go test ./internal/model/ -run TestRole` 通过

## Implementation Notes

Item 19. Change `TableName()` returns in `role.go`. Add `RENAME TABLE` statements to both schema files. Update `role_test.go` assertions. Both schema files must stay in sync (per CLAUDE.md).
