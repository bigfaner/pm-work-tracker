---
id: "3.1"
title: "Extract shared transaction & bizKey helpers"
priority: "P1"
estimated_time: "3h"
dependencies: ["2.gate"]
status: pending
breaking: true
---

# 3.1: Extract shared transaction & bizKey helpers

## Description

Create shared `DBTransactor` interface replacing two duplicate definitions, and shared `ParseBizKeyParam` + `ResolveBizKey` helpers replacing 7 duplicate parse/resolve functions across handlers.

## Reference Files

- `backend/internal/pkg/repo/transactor.go` (new) — DBTransactor interface
- `backend/internal/pkg/handler/bizkey.go` (new) — ParseBizKeyParam + ResolveBizKey
- `backend/internal/service/team_service.go` — uses TransactionDB
- `backend/internal/service/item_pool_service.go` — uses dbTransactor
- `backend/internal/handler/main_item_handler.go` — parseBizKey
- `backend/internal/handler/sub_item_handler.go` — parseSubBizKey, resolveSubID
- `backend/internal/handler/admin_handler.go` — parseUserBizKey
- `backend/internal/handler/role_handler.go` — parseRoleBizKey
- `backend/internal/handler/team_handler.go` — resolveUserBizKey
- `backend/internal/handler/progress_handler.go` — resolveSubID
- `backend/internal/handler/item_pool_handler.go` — resolvePoolID
- `docs/features/schema-alignment-cleanup/design/tech-design.md` — Interfaces 1, 2, 3

## Acceptance Criteria

- [ ] `grep -rn "dbTransactor" backend/` returns zero results
- [ ] `grep -rn "TransactionDB" backend/` returns zero results (except in transactor.go)
- [ ] `grep -rn "parseBizKey\|parseSubBizKey\|parseUserBizKey\|parseRoleBizKey\|resolveSubID\|resolvePoolID\|resolveUserBizKey" backend/internal/handler/` only in bizkey.go
- [ ] `go build ./...` compiles
- [ ] `go test ./internal/handler/` passes
- [ ] `go test ./internal/service/` passes

## User Stories

**Story 3: 代码库可维护性提升**
- Given 代码库中存在重复的 `TransactionDB` / `dbTransactor` 接口
- When 合并为单一共享接口后
- Then `grep -r "dbTransactor" backend/` 返回零结果
- And 所有使用方编译通过且测试通过

## Implementation Notes

Items 10 and 12 combined. Create two new files with shared interfaces/helpers, then update all call sites to import from the shared locations. Delete local definitions. See tech-design.md Interfaces 1-3 for exact signatures. TDD: write tests for ParseBizKeyParam and ResolveBizKey first, then implement.
