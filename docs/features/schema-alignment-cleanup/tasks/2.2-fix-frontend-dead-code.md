---
id: "2.2"
title: "Fix frontend dead code"
priority: "P1"
estimated_time: "1h"
dependencies: ["1.gate"]
status: pending
breaking: false
---

# 2.2: Fix frontend dead code

## Description

Replace `console.error` in Axios interceptor with toast notifications, and fix test data where `Role.id` is numeric instead of string bizKey.

## Reference Files

- `frontend/src/api/client.ts` — Item 8: console.error → toast
- `frontend/src/types/index.test.ts` — Item 9: Role test data
- `docs/features/schema-alignment-cleanup/design/tech-design.md` — Items 8-9 specification

## Acceptance Criteria

- [ ] `grep -n 'console.error' frontend/src/api/client.ts` returns zero results
- [ ] `npx vitest run types/index.test.ts` passes
- [ ] Test data Role.id is string type (e.g., `"role-1"`)
- [ ] Toast notifications work for API errors

## User Stories

**Story 2.5: 清理死代码以降低认知负担**
- Given deprecated console.error and incorrect test data
- When fixes applied
- Then no console.error in client.ts and test data matches actual types

## Implementation Notes

Item 8: Create `frontend/src/lib/toast.ts` shim that exports standalone `showToast(message, variant)`. The shim stores a module-level reference set by `ToastProvider` on mount. The Axios interceptor calls `showToast(message, 'error')` directly. This avoids importing React hooks into a non-React module. Item 9: Change `id: 1` to `bizKey: "1"` in Role test data.
