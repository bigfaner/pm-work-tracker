---
status: "completed"
started: "2026-04-26 20:31"
completed: "2026-04-26 20:35"
time_spent: "~4m"
---

# Task Record: 1.2 Fix filter type mismatch

## Summary
Fix P0 bug in filter_helpers.go where assigneeKey *string was passed raw into SQL WHERE clause comparing against BIGINT column. Added strconv.ParseInt conversion before SQL query; on parse failure applies WHERE 1=0 (fail-closed) to return zero results instead of all items.

## Changes

### Files Created
无

### Files Modified
- backend/internal/repository/gorm/filter_helpers.go
- backend/internal/repository/gorm/main_item_repo_test.go

### Key Decisions
- Used strconv.ParseInt directly rather than pkg.ParseID to avoid unnecessary import for a single conversion in an unexported function
- Fail-closed: invalid assigneeKey returns zero results (WHERE 1=0) rather than skipping the filter (which would return all items - authorization bypass)

## Test Results
- **Passed**: 87
- **Failed**: 0
- **Coverage**: 62.8%

## Acceptance Criteria
- [x] go test ./internal/repository/gorm/ -run TestFilter passes
- [x] Filter with valid string bizKey returns correct subset
- [x] Filter with invalid string returns zero results (WHERE 1=0)
- [x] Behavior is consistent across MySQL and SQLite

## Notes
无
