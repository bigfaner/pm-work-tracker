---
id: "4.3"
title: "Unify soft-delete scope & frontend fixes"
priority: "P2"
estimated_time: "1.5h"
dependencies: ["3.gate"]
status: pending
breaking: true
---

# 4.3: Unify soft-delete scope & frontend fixes

## Description

Replace inline `WHERE deleted_flag = 0` with `NotDeleted` scope in `team_repo` and `role_repo`. Fix `TableRow.mainItemId` type from `number | null` to `string | null`. Extract shared `formatDateOnly` utility.

## Reference Files

- `backend/internal/repository/gorm/team_repo.go` — inline deleted_flag checks (Item 22)
- `backend/internal/repository/gorm/role_repo.go` — inline deleted_flag checks (Item 22)
- `backend/internal/repository/gorm/scopes.go` — NotDeleted scope
- `frontend/src/types/index.ts` — TableRow.mainItemId type (Item 23)
- `frontend/src/pages/TableViewPage.tsx` — mainItemId usage
- `frontend/src/lib/format.ts` — formatDateOnly function (Item 24)
- `frontend/src/pages/TeamDetailPage.tsx` — local date wrapper
- `frontend/src/pages/TeamManagementPage.tsx` — local date wrapper
- `frontend/src/pages/RoleManagementPage.tsx` — local date wrapper
- `docs/features/schema-alignment-cleanup/design/tech-design.md` — Items 22, 23, 24

## Acceptance Criteria

- [ ] `grep -rn "deleted_flag.*=.*0" backend/internal/repository/` returns zero results
- [ ] All repos use `.Scopes(NotDeleted)` instead
- [ ] `grep -n "mainItemId.*number" frontend/src/types/index.ts` returns zero results
- [ ] `npx tsc --noEmit` passes
- [ ] `formatDateOnly` defined once in `lib/format.ts`
- [ ] `go test ./internal/repository/gorm/` passes
- [ ] `npx vitest run` passes

## User Stories

**Story 3: 代码库可维护性提升** (soft-delete scope)
- Given inline deleted_flag checks 和 scope 混用
- When 统一后
- Then 所有软删除查询使用一致的模式

**Story 4: 前端类型一致性** (TableRow type, formatDate)

## Implementation Notes

Items 22, 23, 24 combined. Backend: replace inline SQL with scope call. Frontend: change TableRow type and update TableViewPage usage. Extract `formatDateOnly` to shared lib and update 3 team pages to import from there.
