---
id: "3.3"
title: "Unify pagination pattern"
priority: "P2"
estimated_time: "1h"
dependencies: ["2.gate"]
status: pending
breaking: false
---

# 3.3: Unify pagination pattern

## Description

Replace manual pagination logic in `admin_handler.go` and `view_handler.go` with shared `dto.ApplyPaginationDefaults`.

## Reference Files

- `backend/internal/handler/admin_handler.go:159-175` — manual parsePagination
- `backend/internal/handler/view_handler.go:97-107` — manual offset calculation
- `backend/internal/dto/pagination.go` — ApplyPaginationDefaults

## Acceptance Criteria

- [ ] No manual pagination logic in `admin_handler.go` or `view_handler.go`
- [ ] Both use `dto.ApplyPaginationDefaults`
- [ ] `go test ./internal/handler/` passes

## User Stories

**Story 3: 代码库可维护性提升**
- Given handler 中存在手动分页逻辑
- When 替换为共享 helper 后
- Then 分页逻辑只有一处实现

## Implementation Notes

Item 11. Replace `parsePagination` and manual offset calculation with `dto.ApplyPaginationDefaults(page, pageSize)`. Straightforward refactor with existing helper.
