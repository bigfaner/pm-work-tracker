---
id: "2.1"
title: "Remove backend dead code"
priority: "P1"
estimated_time: "2h"
dependencies: ["1.gate"]
status: pending
breaking: true
---

# 2.1: Remove backend dead code

## Description

Remove deprecated DTOs and dead backend code: 4 deprecated DTOs in `item_dto.go`, unused assignment `_ = team.PmKey`, dead nil checks in handlers, dead nil-slice initialization, redundant GORM column tags.

## Reference Files

- `backend/internal/dto/item_dto.go` — Items 3: deprecated DTOs
- `backend/internal/service/team_service.go:181` — Item 4: unused assignment
- `backend/internal/handler/item_pool_handler.go` — Item 5: dead nil checks
- `backend/internal/handler/progress_handler.go` — Item 5: dead nil checks
- `backend/internal/service/team_service.go:343-345` — Item 6: dead nil-slice init
- `backend/internal/repository/gorm/role_repo.go:129-132` — Item 7: redundant column tags

## Acceptance Criteria

- [ ] `grep -rn "Deprecated" backend/internal/dto/item_dto.go` returns zero results
- [ ] `grep -n '_ = team.PmKey' backend/internal/service/team_service.go` returns zero results
- [ ] Dead nil checks removed from `item_pool_handler.go` and `progress_handler.go`
- [ ] `grep -n '= \[\]string{}' backend/internal/service/team_service.go` target line removed
- [ ] `grep -n 'column:' backend/internal/repository/gorm/role_repo.go` returns zero results
- [ ] `go build ./...` compiles without errors
- [ ] `go vet ./internal/service/` no warnings

## User Stories

**Story 2.5: 清理死代码以降低认知负担**
- Given `item_dto.go` 中存在 4 个标记为 deprecated 的 DTO
- When 删除完成后
- Then `grep -rn "Deprecated" backend/internal/dto/item_dto.go` 返回零结果
- And `go build ./...` 编译通过（无引用断裂）

## Implementation Notes

Items 3-7 combined. Each is a simple deletion. Keep `MainItemSummaryDTO` (used by `report_service.go`). Remove only the deprecated DTOs and dead code. Verify compilation after each deletion batch.
