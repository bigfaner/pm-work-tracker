---
id: "3.1"
title: "Create Textarea component and migrate usage"
priority: "P0"
estimated_time: "2h"
dependencies: []
status: pending
---

# 3.1: Create Textarea Component

## Description

Create a `Textarea` component in `src/components/ui/textarea.tsx` following the same pattern as existing `input.tsx`, then replace all 14 raw `<textarea>` elements across the codebase.

**Component spec**:
- `forwardRef` pattern matching `input.tsx`
- Base classes: `flex w-full rounded-md border border-border-dark bg-white px-3 py-2 text-[13px] text-primary shadow-sm placeholder:text-tertiary focus:outline-none focus:border-primary-500 focus:ring-2 focus:ring-primary-200 min-h-[72px] resize-y`
- Accept `className` prop merged via `cn()`
- Export as `Textarea`

## Reference Files

- Source: `docs/features/code-conventions/design/tech-design.md` (Section 3: Frontend Components)
- Source: `docs/features/code-conventions/design/api-handbook.md` (Textarea interface)
- Pattern: `frontend/src/components/ui/input.tsx`
- All pages using `<textarea>`: search codebase for raw textarea usage

## Acceptance Criteria

- [ ] `src/components/ui/textarea.tsx` exists with forwardRef pattern
- [ ] Component test exists and passes
- [ ] All 14 raw `<textarea>` replaced with `<Textarea>`
- [ ] No visual regression (same styling as before)
- [ ] All frontend tests pass

## Implementation Notes

- TDD: write component test first (mirrors input.test.tsx pattern)
- Use `cn()` from existing utils for className merge
- After creating component, find-and-replace raw textarea usage across all pages
- Verify min-h-[72px] and resize-y are in base classes
