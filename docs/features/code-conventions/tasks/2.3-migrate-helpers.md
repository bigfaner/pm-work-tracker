---
id: "2.3"
title: "Migrate backend code to use shared helpers"
priority: "P0"
estimated_time: "3h"
dependencies: ["2.1"]
status: pending
---

# 2.3: Migrate Backend Code to Shared Helpers

## Description

Replace all inline implementations with the shared helpers created in task 2.1.

**1. MapNotFound migration**:
- Remove 5 domain-specific mapXxxNotFound functions from service files
- Replace with `errors.MapNotFound(err, apperrors.ErrXxx)`
- Replace 8 inline not-found checks

**2. ApplyPaginationDefaults migration**:
- Replace 6 inline pagination blocks in handlers
- Remove corresponding logic from repos (repos accept pre-computed offset/limit)

**3. ParseDate migration**:
- Replace 11 inline `time.Parse("2006-01-02", ...)` calls with `dates.ParseDate()`

## Reference Files

- Source: `docs/features/code-conventions/design/tech-design.md` (Section 1)
- Source: `docs/features/code-conventions/design/api-handbook.md` (usage examples)
- Service files: `backend/internal/service/*.go`
- Handler files: `backend/internal/handler/*_handler.go`
- Repo files: `backend/internal/repository/gorm/*.go`

## Acceptance Criteria

- [ ] 0 remaining mapXxxNotFound functions (each replaced by errors.MapNotFound)
- [ ] 0 remaining inline pagination default blocks in handlers
- [ ] 0 remaining inline `time.Parse("2006-01-02", ...)` calls
- [ ] All backend tests pass: `go test ./...`
- [ ] golangci-lint dupl violations reduced by 50%+ vs before

## Implementation Notes

- Run full backend test suite before AND after migration
- One service file at a time, running tests between each
- Verify no behavior change — helpers produce identical results
- Update imports in each modified file
