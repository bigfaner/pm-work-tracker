---
id: "3.2"
title: "Create PrioritySelect component and migrate usage"
priority: "P0"
estimated_time: "1.5h"
dependencies: []
status: pending
---

# 3.2: Create PrioritySelect Component

## Description

Create `PrioritySelectItems` component in `src/components/shared/PrioritySelect.tsx` that provides the 3 priority `<SelectItem>` options (P1, P2, P3). Replace all 21 inline priority option groups across the codebase.

**Component spec**:
- Export `PrioritySelectItems` function component
- Renders exactly 3 `<SelectItem>` options: P1, P2, P3
- Caller provides `<Select>`, `<SelectTrigger>`, `<SelectContent>`

## Reference Files

- Source: `docs/features/code-conventions/design/tech-design.md` (Section 3: PrioritySelect)
- Source: `docs/features/code-conventions/design/api-handbook.md` (PrioritySelect interface)
- Search: all files containing `<SelectItem value="P1">` or similar

## Acceptance Criteria

- [ ] `src/components/shared/PrioritySelect.tsx` exists
- [ ] Component renders 3 SelectItem options (P1, P2, P3)
- [ ] Component test exists and passes
- [ ] All 21 inline priority option groups replaced with `<PrioritySelectItems />`
- [ ] All frontend tests pass

## Implementation Notes

- TDD: write test first verifying 3 options render
- Component is a fragment wrapper — no Select/Trigger/Content included
- Search for `SelectItem value="P1"` and `SelectItem value="P2"` to find all usages
- After replacement, verify no leftover inline P1/P2/P3 SelectItem groups
