---
status: "completed"
started: "2026-04-22 19:12"
completed: "2026-04-22 19:30"
time_spent: "~18m"
---

# Task Record: 6.1 并发编码生成集成测试

## Summary
编写并修复并发编码生成集成测试。新增 concurrent_code_test.go，包含 TestConcurrentNextCode 和 TestConcurrentNextSubCode 两个测试。修复根本原因：SQLite 不支持 SELECT FOR UPDATE，改为在事务内对 teams.item_seq / main_items.sub_item_seq 做原子自增（真实写操作），强制 SQLite 进入写锁模式以序列化并发调用。同时保留对直接插入高序号记录的兼容（skips_gaps 场景）。

## Changes

### Files Created
- backend/internal/repository/gorm/concurrent_code_test.go

### Files Modified
- backend/internal/model/team.go
- backend/internal/model/main_item.go
- backend/internal/repository/gorm/main_item_repo.go
- backend/internal/repository/gorm/sub_item_repo.go

### Key Decisions
- SQLite 不支持 SELECT FOR UPDATE，改用原子自增计数器列（item_seq / sub_item_seq）作为写锁触发器
- 保留 MAX(code) 扫描作为 fallback，确保直接插入高序号记录时不产生重复编码（skips_gaps 兼容）
- 并发测试使用 file::memory:?cache=shared + SetMaxOpenConns(1) 确保多 goroutine 共享同一连接

## Test Results
- **Passed**: 2
- **Failed**: 0
- **Coverage**: 77.0%

## Acceptance Criteria
- [x] 同一团队下 2 个 goroutine 并发调用 NextCode()，两者返回不同编码且均非空
- [x] 同一主事项下 2 个 goroutine 并发调用 NextSubCode()，两者返回不同编码
- [x] 测试在 SQLite 内存数据库下通过（表锁保证正确性）
- [x] cd backend && go test ./internal/repository/gorm/... -run TestConcurrent 通过

## Notes
无
