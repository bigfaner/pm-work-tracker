---
status: "completed"
started: "2026-04-22 18:45"
completed: "2026-04-22 18:53"
time_spent: "~8m"
---

# Task Record: 4.1 数据迁移程序：重写现有编码

## Summary
实现数据迁移程序 backend/cmd/migrate/rewrite_codes.go，按 team_id 分组重写 main_items.code 为 {TEAM_CODE}-{seq:05d}，按 main_item_id 分组生成 sub_items.code 为 {main_code}-{seq:02d}，整个迁移包裹在单个事务内，支持 dry-run 模式，迁移后执行验证断言

## Changes

### Files Created
- backend/cmd/migrate/rewrite_codes.go
- backend/cmd/migrate/rewrite_codes_test.go

### Files Modified
无

### Key Decisions
- 将 migrate() 逻辑从 main/run 中抽离为独立函数，使其可被测试
- dry-run 通过返回 errDryRun sentinel 触发事务回滚，保持代码路径一致
- 验证失败路径通过插入孤立记录（orphaned team_id/main_item_id）触发，无需 mock DB

## Test Results
- **Passed**: 9
- **Failed**: 0
- **Coverage**: 80.9%

## Acceptance Criteria
- [x] 迁移程序位于 backend/cmd/migrate/rewrite_codes.go
- [x] 按 team_id 分组，组内按 id 升序，分配 {team.Code}-{seq:05d}
- [x] 按 main_item_id 分组，组内按 id 升序，生成 {main_item.Code}-{sub_seq:02d}
- [x] 整个迁移包裹在单个事务内，任意步骤失败则回滚
- [x] 迁移后验证：SELECT COUNT(*) FROM main_items WHERE code LIKE 'MI-%' 返回 0
- [x] 迁移后验证：SELECT COUNT(*) FROM sub_items WHERE code IS NULL OR code = '' 返回 0

## Notes
无
