---
status: "completed"
started: "2026-04-27 22:13"
completed: "2026-04-27 22:28"
time_spent: "~15m"
---

# Task Record: 2.2 Admin handler and router registration

## Summary
Implemented ResetPassword and DeleteUser handler methods on AdminHandler and registered two new routes (PUT /users/:userId/password, DELETE /users/:userId) in the router within the admin group with user:update permission. Both routes go through auth middleware and permission gating. Added 10 handler unit tests covering success, not-found, validation, self-delete (422), and service error paths.

## Changes

### Files Created
无

### Files Modified
- backend/internal/handler/admin_handler.go
- backend/internal/handler/admin_handler_test.go
- backend/internal/handler/router.go

### Key Decisions
- ResetPassword handler uses ResetPasswordReq DTO with binding validation (min=8,max=64) rather than manual validation
- DeleteUser handler uses middleware.GetUserID(c) for callerID, consistent with ToggleUserStatus pattern
- Both new routes use deps.perm("user:update") permission, same as existing UpdateUser and ToggleUserStatus
- Mock ResetPassword and SoftDeleteUser methods in test now capture call parameters for assertions

## Test Results
- **Passed**: 10
- **Failed**: 0
- **Coverage**: 84.5%

## Acceptance Criteria
- [x] PUT /admin/users/:userId/password returns 200 on success with correct response shape
- [x] PUT /admin/users/:userId/password returns 400 on invalid input
- [x] PUT /admin/users/:userId/password returns 404 on user not found
- [x] DELETE /admin/users/:userId returns 200 on success
- [x] DELETE /admin/users/:userId returns 404 on user not found
- [x] DELETE /admin/users/:userId returns 422 on self-delete
- [x] Both routes require user:update permission (403 without it)
- [x] Handler unit tests pass (using httptest + mock service)

## Notes
无
