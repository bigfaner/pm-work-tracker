---
status: "completed"
started: "2026-04-19 13:57"
completed: "2026-04-19 14:00"
time_spent: "~3m"
---

# Task Record: 1.2 Data Models (Role, RolePermission, TeamMember Changes)

## Summary
Added Role and RolePermission GORM models, added nullable RoleID field to TeamMember for RBAC migration transition. User model unchanged (CanCreateTeam kept temporarily per task spec).

## Changes

### Files Created
- backend/internal/model/role.go
- backend/internal/model/role_test.go

### Files Modified
- backend/internal/model/team.go

### Key Decisions
- Role uses BaseModel (includes soft delete via DeletedAt) following existing model conventions
- RolePermission uses plain ID without soft delete - permissions are hard-deleted when updating role
- TeamMember.RoleID is *uint (nullable pointer) for migration transition period
- RolePermission unique constraint uses GORM composite uniqueIndex tag idx_role_permission on (RoleID, PermissionCode)

## Test Results
- **Passed**: 9
- **Failed**: 0
- **Coverage**: 100.0%

## Acceptance Criteria
- [x] Role model with fields: ID, Name (unique), Description, IsPreset, CreatedAt, UpdatedAt, DeletedAt
- [x] RolePermission model with fields: ID, RoleID (FK), PermissionCode, unique constraint on (RoleID, PermissionCode)
- [x] TeamMember model gains RoleID *uint field (nullable for migration transition)
- [x] User model keeps IsSuperAdmin bool; CanCreateTeam remains temporarily
- [x] GORM table names: roles, role_permissions
- [x] No behavioral changes to existing code — only model struct additions

## Notes
无
