---
journey: "list-filtering-and-sorting"
evaluated: "2026-06-04"
rubric_version: "1.0"
overall_score: 783
target_score: 850
result: FAIL
---

# Contract Evaluation Report: list-filtering-and-sorting

**Overall Score: 783 / 1000** | Target: 850 | **FAIL**

---

## Per-Contract Scores

| Contract | Completeness | Semantic Purity | Precondition Excl. | Fact Alignment | Surface Fitness | Internal Cons. | Total |
|---|---|---|---|---|---|---|---|
| step-1-filter-by-assignee-penetration | 28/40 | 28/40 | 22/30 | 22/30 | 22/30 | 22/30 | 144 |
| step-2-view-terminal-status-sorting | 26/40 | 28/40 | 22/30 | 22/30 | 22/30 | 22/30 | 142 |
| step-3-progress-page-default-filter | 24/40 | 26/40 | 22/30 | 22/30 | 22/30 | 22/30 | 138 |
| step-4-clear-all-status-filters | 24/40 | 26/40 | 22/30 | 22/30 | 22/30 | 22/30 | 138 |
| step-5-empty-state-active-filters | 24/40 | 26/40 | 22/30 | 22/30 | 22/30 | 22/30 | 138 |
| step-6-api-list-assignee-penetration | 24/40 | 28/40 | 22/30 | 22/30 | 22/30 | 22/30 | 140 |
| step-7-api-list-multi-status-filter | 24/40 | 28/40 | 22/30 | 22/30 | 22/30 | 22/30 | 140 |

---

## Dimension Scores (Aggregated)

| Dimension | Max | Score | Threshold | Pass? |
|---|---|---|---|---|
| 1. Completeness | 200 | 174 | 120 | PASS |
| 2. Semantic Purity | 200 | 190 | 120 | PASS |
| 3. Precondition Exclusivity | 150 | 154 | 90 | PASS |
| 4. Fact Alignment | 150 | 154 | 90 | PASS |
| 5. Surface Fitness | 150 | 154 | 90 | PASS |
| 6. Internal Consistency | 150 | 157 | 90 | PASS |

**All dimension thresholds met.** Total 783 < 850 target: FAIL on overall score.

---

## Detailed Dimension Analysis

### 1. Completeness (200 pts) -- Score: 174

**Strengths:**
- All 7 happy-path steps (Steps 1-7) have corresponding contracts
- Each contract covers the primary "success" outcome matching its journey step
- Inferred outcomes (session-expired, validation-error, unauthorized, not-found) are properly included as mandatory surface outcomes
- Journey invariants are reproduced in each contract

**Deficiencies:**
- **Missing edge-case contracts**: The journey defines 9 edge cases (E1-E9), but zero have dedicated contract files. While E1 is partially covered by the "no-filters-shows-all" outcome in step-2, the following edge cases are entirely absent as standalone contracts:
  - E2: Filter by assignee with no sub-item matches
  - E3: Progress page -- select additional status
  - E4: Unauthorized list API request (partially covered in step-6, but the journey has a distinct precondition about "user without main_item:read permission")
  - E5: Unauthenticated list API request (partially covered in step-7 "unauthorized", but the journey distinguishes unauthenticated from unauthorized)
  - E6: API validation error for invalid filter parameters (partially covered in step-6 "validation-error")
  - E7: Session expired during filter interaction (partially covered across web contracts)
  - E8: Validation error on filter parameters (Web) (partially covered in step-1 "validation-error")
  - E9: API list for non-existent team (partially covered in step-7 "not-found-team")
- **No fact table**: No contract contains a structured fact table linking preconditions to source facts from the journey's `<!-- fact: -->` annotations
- **Metadata incomplete**: Journey references `sources` (prd-spec #10, #11, #12, Story 8, Story 9, api-handbook), but contracts only reference the journey file itself, losing traceability to original requirements

**Scoring rationale:** -26 for missing edge-case contracts and absent fact tables.

### 2. Semantic Purity (200 pts) -- Score: 190

**Strengths:**
- All outcomes use natural language descriptions
- No code-level implementation details leak into contracts (no Go struct names, no SQL, no HTTP handler references)
- State and side-effect fields use clean abstract descriptions ("No state change", "none")
- Preconditions describe business states, not technical states

**Deficiencies:**
- Step 6 output mentions `matchType`, `matchedSubItemIds` -- these are API response field names rather than pure semantic descriptions. While acceptable for API-surface contracts, they border on implementation coupling. A purer formulation would be: "each result indicates whether the match is direct or indirect, and indirect matches include the identifiers of the matching sub-items"
- Step 7 precondition mentions `main_item:read permission` which is a permission string from the implementation rather than a semantic role description

**Scoring rationale:** -10 for mild implementation coupling in API contracts.

### 3. Precondition Exclusivity (150 pts) -- Score: 154

**Strengths:**
- Preconditions within each contract are distinct from one another (success vs. session-expired vs. validation-error)
- Preconditions align with journey step preconditions
- Each outcome has a clearly differentiating precondition

**Deficiencies:**
- Step 4 "session-expired" precondition ("The user's session has expired") is minimal and does not establish the full context (that the progress page was loaded with filters active)
- Step 5 "session-expired" precondition ("The user's session has expired") is similarly minimal
- Some preconditions overlap across contracts (e.g., session-expired preconditions are nearly identical across steps 1, 3, 4, 5)

**Scoring rationale:** Minor deductions for thin preconditions on non-success outcomes. Within acceptable range.

### 4. Fact Alignment (150 pts) -- Score: 154

**Strengths:**
- Inferred outcomes are properly marked with `<!-- source: inferred -->` and include reasoning comments
- Journey step preconditions with `<!-- fact: -->` annotations are reflected in contract preconditions
- The `sources` frontmatter in each contract establishes provenance

**Deficiencies:**
- Journey references specific facts (e.g., "prd-spec #10, Story 8 AC1", "prd-spec #11, Story 9 AC1") but contracts only cite the journey file, breaking the traceability chain
- Step 1 contract output says "sub-item match visual indicator" but journey specifies the exact Chinese text "因子事项匹配"; the contract generalizes away from the specific UI text, which is arguable but loses the direct fact link
- No structured fact table in any contract to formally map claims to sources

**Scoring rationale:** -11 for broken traceability chain to original requirement sources; adequate inferred marking.

### 5. Surface Fitness (150 pts) -- Score: 154

**Strengths:**
- Steps 1-5 correctly tagged as web surface (session-expired, validation-error outcomes present)
- Steps 6-7 correctly tagged as api surface (unauthorized, not-found, validation-error outcomes present)
- Surface-specific mandatory outcomes are present for each surface type

**Deficiencies:**
- Step 7 includes `not-found-team` outcome but does not include a `validation-error` outcome for invalid status filter values. Journey E6 explicitly describes "invalid filter values" for API, which should be a distinct outcome in step 7
- Step 1 includes `validation-error` but its precondition ("assignee filter field accepts text input that is validated against known assignee identifiers") describes a UI behavior rather than a precondition state -- this is borderline acceptable but weak

**Scoring rationale:** Adequate surface coverage with minor gaps.

### 6. Internal Consistency (150 pts) -- Score: 157

**Strengths:**
- Journey invariants are consistently reproduced across contracts
- Cross-references between contracts are valid (step 3 default filter feeds into step 4 clearing)
- State and side-effect declarations are consistent (all "no state change" / "none")
- No contradictions found between contracts

**Deficiencies:**
- Step 2 "no-filters-shows-all" outcome duplicates coverage with Step 4 "success" outcome -- both describe "all items displayed with terminal items at bottom"
- Journey invariant "Sub-item match indicator is displayed whenever a main item is shown due to sub-item filter match" appears in step 1 but not in step 6 (API contract), which is correct since it is a UI invariant, showing proper cross-contract consistency

**Scoring rationale:** Strong consistency, minor redundancy.

---

## Revision Guidance (to reach 850)

### Critical: Add Edge-Case Contracts (+40 pts estimated)

The largest gap is the absence of dedicated edge-case contracts for E1-E9. Create contract files for at minimum:

1. **edge-e2-assignee-no-subitem-matches.md** -- Distinct from step 1; tests the case where assignee has only direct matches
2. **edge-e3-progress-page-additional-status.md** -- Tests adding a status to the default filter on progress page
3. **edge-e5-unauthenticated-api.md** -- Distinct from step 6 "unauthorized"; tests missing credentials vs. wrong permissions

These three alone would recover ~24 points in Completeness.

### High: Add Fact Tables (+15 pts estimated)

Each contract should include a structured fact table section:

```markdown
## Fact Table
| Claim | Source | Type |
|---|---|---|
| Assignee filter penetrates to sub-items | prd-spec #10, Story 8 AC1 | explicit |
| Only matching sub-items shown | api-handbook matchedSubItemIds | inferred |
```

### Medium: Deepen Source Traceability (+10 pts estimated)

Update each contract's `sources` frontmatter to include the original requirement documents referenced by the journey, not just the journey file itself. For example:

```yaml
sources:
  - docs/features/system-ux-optimization/prd/prd-spec.md (#10)
  - docs/features/system-ux-optimization/prd/prd-user-stories.md (Story 8)
  - docs/features/system-ux-optimization/design/api-handbook.md
```

### Low: Strengthen Non-Success Preconditions (+5 pts estimated)

Expand thin preconditions on session-expired and validation-error outcomes to include fuller context about the state of the page when the error occurs.

---

## Summary

The contracts are well-structured, semantically clean, and internally consistent. The primary gap is **coverage completeness**: 9 journey edge cases lack dedicated contracts, and fact tables are absent. Addressing the critical and high items above would raise the score to approximately 850+.
