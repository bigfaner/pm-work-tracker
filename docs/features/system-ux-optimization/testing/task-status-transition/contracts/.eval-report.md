# Contract Eval Report: task-status-transition

**Journey**: task-status-transition (High risk)
**Surfaces**: web, api
**Contracts evaluated**: 7 (Steps 1-7; Edge Cases E1-E10 are covered as outcomes within the step contracts)
**Date**: 2026-06-04

---

## Scoring Methodology

Each contract scored on 6 dimensions (1000 points total):

| Dimension | Max | Threshold |
|-----------|-----|-----------|
| Completeness | 200 | 120 |
| Semantic Purity | 200 | 120 |
| Precondition Exclusivity | 150 | 90 |
| Fact Alignment | 150 | 90 |
| Surface Fitness | 150 | 90 |
| Internal Consistency | 150 | 90 |
| **Total** | **1000** | **>= 850** |

---

## Per-Contract Scores

| Contract | Score | Status |
|----------|-------|--------|
| step-1-trigger-status-transition-error | 885/1000 | PASS |
| step-2-successful-status-transition-non-terminal | 870/1000 | PASS |
| step-3-terminal-status-transition-confirmation | 905/1000 | PASS |
| step-4-open-conversion-form-defaults | 870/1000 | PASS |
| step-5-submit-conversion-form-success | 895/1000 | PASS |
| step-6-close-reopen-conversion-form | 845/1000 | FAIL |
| step-7-submit-todo-to-main-item-conversion | 880/1000 | PASS |

## Overall: 865/1000 — PASS (arithmetic mean; 6 of 7 contracts PASS)

---

## Detailed Scoring

### step-1-trigger-status-transition-error (885/1000) — PASS

| Dimension | Score | Notes |
|-----------|-------|-------|
| Completeness | 180 | Covers all journey outcomes for Step 1 plus E1 (concurrent-edit-conflict). Includes unauthorized, session-expired, validation-error. Missing: no explicit `surface` metadata tag in frontmatter. |
| Semantic Purity | 180 | Natural language descriptions. Minor coupling: Input for `unauthorized-attempt` references `PUT /api/v1/teams/:teamId/main-items/:itemId/status` — implementation detail leaking into contract. |
| Precondition Exclusivity | 135 | Preconditions are distinct across outcomes. `concurrent-edit-conflict` precondition is well-isolated from `invalid-transition`. Good. |
| Fact Alignment | 140 | Inferred outcomes clearly marked with `<!-- source: inferred -->` and reasoning comments. Claims trace to journey and fact tables. |
| Surface Fitness | 130 | Contract covers web and api surfaces but no explicit `surface` tag per outcome. Step 1 in journey is `<!-- surface: web -->` only; unauthorized and validation-error outcomes are API-level. |
| Internal Consistency | 120 | Invariants match journey invariants. Cross-references consistent. `validation-error` outcome in Step 1 is well-defined for the API surface. |

**Strengths**: Most comprehensive contract. Excellent coverage of concurrent-edit-conflict (E1). Clear separation of concerns between invalid-transition and concurrent-edit.

**Minor issues**:
- Input for `unauthorized-attempt` references specific HTTP method/path — should abstract to "sends a status transition API request"
- No per-outcome surface tags

---

### step-2-successful-status-transition-non-terminal (870/1000) — PASS

| Dimension | Score | Notes |
|-----------|-------|-------|
| Completeness | 175 | Covers success, unauthorized, session-expired. Missing: no validation-error outcome for malformed request body (present in Step 1). Missing concurrent-edit outcome (only covered in Step 1). |
| Semantic Purity | 175 | Mostly clean. Unauthorized input references specific API path. |
| Precondition Exclusivity | 130 | Preconditions are clear and distinct. Success precondition properly includes "no concurrent modification". |
| Fact Alignment | 135 | Inferred outcomes properly marked. State claims reference database-level changes. |
| Surface Fitness | 130 | Journey marks Step 2 as web-only, but unauthorized outcome is API-level — appropriate. No explicit surface tags. |
| Internal Consistency | 125 | State description mentions "status history record created" — consistent with implementation. Invariants match journey. |

**Strengths**: Clean success outcome with proper state documentation. Good precondition on concurrent modification.

**Minor issues**:
- Missing concurrent-edit-conflict outcome (relies on Step 1 to cover it, but contract should be self-contained for its own surface)
- Unauthorized input references specific HTTP path

---

### step-3-terminal-status-transition-confirmation (905/1000) — PASS

| Dimension | Score | Notes |
|-----------|-------|-------|
| Completeness | 195 | Covers success, cancelled (E2), sub-items-not-terminal, unauthorized, session-expired. All journey edge cases for terminal transitions captured. |
| Semantic Purity | 185 | Clean natural language. No implementation coupling in success/cancelled/sub-items-not-terminal. Unauthorized input slightly coupled. |
| Precondition Exclusivity | 145 | Excellent: `sub-items-not-terminal` precondition is clearly distinct from `success` (adds "at least one sub-item in non-terminal status"). Cancelled has distinct dialog-open precondition. |
| Fact Alignment | 140 | State claims are specific (completion set to 100, actual_end_date set, status history record). Side-effect documents parent recalculation and linkage evaluation. |
| Surface Fitness | 125 | No explicit surface tags. Journey marks Step 3 as web. Unauthorized is API — appropriate. |
| Internal Consistency | 115 | Side-effect mentions "linkage evaluation triggered" — this is implementation-specific terminology. Could be more abstract. |

**Strengths**: Best contract overall. Comprehensive coverage including the sub-items-not-terminal guard (inferred). Excellent precondition exclusivity. Rich state and side-effect documentation.

**Minor issues**:
- Side-effect uses "linkage evaluation" — borderline implementation coupling
- Unauthorized input could abstract away HTTP path

---

### step-4-open-conversion-form-defaults (870/1000) — PASS

| Dimension | Score | Notes |
|-----------|-------|-------|
| Completeness | 170 | Covers success, session-expired, validation-error. Missing: unauthorized outcome (what happens if user without permission tries to open the form?). |
| Semantic Purity | 180 | Clean descriptions. Output lists specific form behaviors naturally. |
| Precondition Exclusivity | 130 | Preconditions are distinct. Success and validation-error share "conversion form is open" but differ on required field state. |
| Fact Alignment | 135 | Output accurately reflects journey Step 4 expectations. Inferred outcomes marked. |
| Surface Fitness | 130 | Step 4 is web surface only — contract matches. No API outcomes needed here. |
| Internal Consistency | 125 | Invariants correctly reference description field disabled rule. |

**Strengths**: Clear output specification with specific UI behaviors (disabled, greyed out, required markers). Good alignment with journey.

**Minor issues**:
- Missing unauthorized outcome — what if user lacks permission to open the conversion form?
- `validation-error` is slightly misleading name for "form shows required markers with disabled submit" — not really a validation error, more of a "form-not-ready" state

---

### step-5-submit-conversion-form-success (895/1000) — PASS

| Dimension | Score | Notes |
|-----------|-------|-------|
| Completeness | 185 | Covers success, backend-validation-failure (E4), unauthorized, session-expired, validation-error. Excellent coverage. |
| Semantic Purity | 180 | Clean language. Backend-validation-failure output is clear about form state preservation. |
| Precondition Exclusivity | 135 | backend-validation-failure vs validation-error preconditions are distinct (filled fields vs empty fields). |
| Fact Alignment | 140 | State claims reference snowflake bizKey and auto-generated code. Side-effect mentions linkage evaluation. Inferred outcomes well-documented. |
| Surface Fitness | 130 | Step 5 is web surface. Unauthorized and validation-error extend to API appropriately. |
| Internal Consistency | 125 | Invariants match. Cross-reference to journey E4 is implicit but clear. |

**Strengths**: Clear distinction between backend-validation-failure (fields preserved) and validation-error (field-level error near empty field). Good state documentation for new sub-item creation.

**Minor issues**:
- `success` outcome references "snowflake bizKey" in State — implementation detail
- Unauthorized input references "Member-role user submits a conversion form API request" — should clarify which API endpoint abstraction

---

### step-6-close-reopen-conversion-form (845/1000) — FAIL

| Dimension | Score | Notes |
|-----------|-------|-------|
| Completeness | 145 | Covers success (close-reopen), after-successful-submission (E5), session-expired. Missing: unauthorized (what if user session expired but they had permission? Already covered. But what about a user who never had permission trying to open a form?). Missing: validation-error is absent (form open always succeeds for authorized users — arguably not needed, but the surface requires it). |
| Semantic Purity | 175 | Clean natural language throughout. |
| Precondition Exclusivity | 130 | Preconditions are distinct. `success` has "partially filled"; `after-successful-submission` has "just submitted successfully". Clear. |
| Fact Alignment | 125 | Claims are reasonable but lack source annotations for some inferences. `after-successful-submission` references E5 which is correct. |
| Surface Fitness | 130 | Web-only step — correct. |
| Internal Consistency | 140 | Invariants correctly reference both form clearing and description disabled rules. Self-consistent. |

**FAIL reasons** (score 845 < 850 threshold):

1. **Completeness gap (145/200)**: The contract covers only 3 outcomes for a step that involves form lifecycle management. Missing:
   - No `unauthorized` outcome — if a user without permission tries to open any form
   - No `validation-error` outcome — while arguably the form just opens, the web surface mandatory outcome requires it; could document "form always opens for authorized users" or add the edge case

2. **Fact alignment (125/150)**: The `after-successful-submission` outcome lacks a source annotation explaining how it maps to journey E5. Other contracts use `<!-- source: inferred -->` with reasoning comments more consistently.

3. **Thin contract overall**: Step 6 is inherently simpler, but the contract does not adequately distinguish itself from Step 4's form-opening behavior. Could benefit from an outcome covering "reopen same form type" vs "open different form type" to ensure cleanup works across form types.

**Attack points for revision**:
- Add `unauthorized` outcome: "User without form access permission attempts to open a conversion form"
- Add source annotations to `after-successful-submission` outcome
- Consider adding a `validation-error` outcome or explicitly documenting why it is not applicable
- Add outcome for "close and reopen different form type" to verify cleanup across form variants

---

### step-7-submit-todo-to-main-item-conversion (880/1000) — PASS

| Dimension | Score | Notes |
|-----------|-------|-------|
| Completeness | 175 | Covers success, required-fields-missing (E3), unauthorized, session-expired, validation-error. Good. Missing: no backend-validation-failure outcome (present in Step 5 but not here — is there no backend validation for main-item conversion?). |
| Semantic Purity | 180 | Clean language. `required-fields-missing` describes UI behavior well. |
| Precondition Exclusivity | 135 | Distinct preconditions. required-fields-missing vs validation-error differentiate UI vs API level. |
| Fact Alignment | 130 | References snowflake bizKey in state — implementation coupling. Inferred outcomes marked. |
| Surface Fitness | 130 | Step 7 is web surface. API outcomes appropriate. |
| Internal Consistency | 130 | Invariants match journey. Form clearing invariant present. |

**Strengths**: Good coverage of E3 (required-fields-missing). Clear distinction between UI-level prevention (disabled button) and API-level rejection.

**Minor issues**:
- State references "auto-generated code and snowflake bizKey" — implementation detail
- Missing backend-validation-failure outcome (could main-item conversion have duplicate name issues similar to sub-item conversion in E4?)
- `required-fields-missing` outcome input says "opens the form without selecting" — this is a form-open action, not a submission action; the outcome name suggests missing fields at submit time but the input describes opening behavior

---

## Summary

### Overall Assessment: 865/1000 — PASS

**Strengths across all contracts**:
1. **Inferred outcomes well-documented**: All inferred outcomes use `<!-- source: inferred -->` with reasoning comments, providing clear traceability.
2. **Journey invariant coverage**: Each contract includes relevant journey invariants, ensuring behavioral constraints are surfaced.
3. **Good separation of concerns**: Distinct outcomes for unauthorized (403), validation-error (400), session-expired, and business logic failures.
4. **State documentation**: Consistent documentation of database state changes, including status history records and side-effects.
5. **Surface-appropriate outcomes**: Web steps include API-level outcomes where relevant (unauthorized, validation-error).

**Systemic issues across contracts**:
1. **No per-outcome surface tags**: Contracts lack explicit `<!-- surface: web -->` or `<!-- surface: api -->` tags on individual outcomes, making it harder to generate surface-specific test scripts.
2. **Implementation coupling in unauthorized inputs**: Several contracts reference specific HTTP paths (e.g., `PUT /api/v1/teams/:teamId/main-items/:itemId/status`) which couples contracts to routing implementation.
3. **Implementation terms in State**: Terms like "snowflake bizKey", "linkage evaluation triggered" leak implementation details into contracts.
4. **Missing explicit frontmatter surface tag**: No `surface` field in YAML frontmatter to declare which surfaces the contract covers.

### Failing Contract: step-6-close-reopen-conversion-form

The only failing contract at 845/1000. Primary deficiencies are completeness (only 3 outcomes for a form-lifecycle step) and weaker fact alignment annotations. Recommended revisions listed in the detailed scoring above.
