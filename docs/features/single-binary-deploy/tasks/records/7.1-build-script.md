---
status: "completed"
started: "2026-04-22 10:34"
completed: "2026-04-22 10:35"
time_spent: "~1m"
---

# Task Record: 7.1 Create scripts/build.sh

## Summary
Created scripts/build.sh with full single-binary build flow: validates dev/prod arg, checks git branch, builds frontend, copies dist to backend embed dir, builds Go binary.

## Changes

### Files Created
- scripts/build.sh

### Files Modified
无

### Key Decisions
- Used set -euo pipefail for strict error handling
- Branch validation uses git -C ROOT_DIR to work from any cwd

## Test Results
- **Passed**: 4
- **Failed**: 0
- **Coverage**: -1.0%

## Acceptance Criteria
- [x] 接受 dev 或 prod 参数，其他参数打印用法并退出码 1
- [x] dev 环境校验当前分支为 dev，prod 校验为 main，不匹配则退出码 1 并打印错误
- [x] 执行 npm ci && npm run build（在 frontend 目录）
- [x] 将 frontend/dist/ 复制到 backend/web/dist/
- [x] 执行 go build -o ../bin/pm-work-tracker-{env} ./cmd/server（在 backend 目录）
- [x] 任意步骤失败立即停止（set -e），非零退出码
- [x] 成功后打印 Build complete: bin/pm-work-tracker-{env}
- [x] 脚本有执行权限（chmod +x）

## Notes
无
