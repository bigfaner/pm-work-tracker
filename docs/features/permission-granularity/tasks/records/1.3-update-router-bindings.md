---
status: "completed"
started: "2026-04-29 23:44"
completed: "2026-04-29 23:47"
time_spent: "~3m"
---

# Task Record: 1.3 更新路由中间件权限码绑定（router.go）

## Summary
Updated 14 route bindings in router.go adminGroup: replaced user:manage_role with user:list/user:assign_role/role:read/role:create/role:update/role:delete per tech-design mapping. user:read changed to user:list for GET /admin/users and GET /admin/teams. Zero user:manage_role residual confirmed by grep.

## Changes

### Files Created
无

### Files Modified
- backend/internal/handler/router.go

### Key Decisions
- Only perm string args changed; no route paths or handler functions touched
- user:list used for both GET /admin/users and GET /admin/teams per tech-design

## Test Results
- **Passed**: 40
- **Failed**: 0
- **Coverage**: 83.0%

## Acceptance Criteria
- [x] GET /admin/users → deps.perm("user:list")
- [x] GET /admin/users/:userId → deps.perm("user:read")
- [x] POST /admin/users → deps.perm("user:assign_role")
- [x] GET /admin/teams → deps.perm("user:list")
- [x] GET /admin/roles → deps.perm("role:read")
- [x] GET /admin/roles/:id → deps.perm("role:read")
- [x] POST /admin/roles → deps.perm("role:create")
- [x] PUT /admin/roles/:id → deps.perm("role:update")
- [x] DELETE /admin/roles/:id → deps.perm("role:delete")
- [x] GET /admin/permissions → deps.perm("role:read")
- [x] user:manage_role zero residual in router.go (grep verified)
- [x] go build ./backend/... passes

## Notes
无
