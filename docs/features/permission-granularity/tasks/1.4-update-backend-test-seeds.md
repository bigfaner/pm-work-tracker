---
id: "1.4"
title: "更新后端测试种子数据（router_test.go + VerifyPresetRoleCodes）"
priority: "P0"
estimated_time: "1h"
dependencies: ["1.1", "1.3"]
status: pending
breaking: false
---

# 1.4: 更新后端测试种子数据（router_test.go + VerifyPresetRoleCodes）

## Description

更新两处后端测试中的权限码引用：

1. `backend/internal/handler/router_test.go`：将测试种子数据中的 `"user:read"` 和 `"user:manage_role"` 替换为新权限码列表
2. `backend/internal/migration/rbac.go` 中的 `VerifyPresetRoleCodes`：更新 pm 角色期望权限码列表

## Reference Files

- `backend/internal/handler/router_test.go` — 测试种子数据（第 52-65 行）
- `backend/internal/migration/rbac.go` — `VerifyPresetRoleCodes` 函数
- `docs/features/permission-granularity/design/tech-design.md` §Interfaces 4 — pm 角色新权限码列表

## Acceptance Criteria

- [ ] `router_test.go` 中 pm 角色种子数据包含 `user:list`、`user:read`、`user:assign_role`、`role:read`、`role:create`、`role:update`、`role:delete`
- [ ] `router_test.go` 中不含 `user:manage_role` 引用
- [ ] `VerifyPresetRoleCodes` 中 pm 角色期望列表与 `seedPresetRoles` 一致
- [ ] `go test ./backend/internal/handler/ -run TestRouter` 通过
- [ ] `go test ./backend/internal/migration/ -run TestVerifyPresetRoleCodes` 通过

## User Stories

Story 4 AC: pm 角色持有完整的 `user:*` 和 `role:*` 权限

## Implementation Notes

- `router_test.go` 第 61 行：将 `"user:read", "user:manage_role"` 替换为 `"user:list", "user:read", "user:assign_role", "role:read", "role:create", "role:update", "role:delete"`
- `VerifyPresetRoleCodes` 中 pm 的 expected 列表同步更新
