---
id: "2.5"
title: "更新前端权限测试（permission-driven-ui.test.tsx）"
priority: "P0"
estimated_time: "1h"
dependencies: ["1.gate", "2.2", "2.3", "2.4"]
status: pending
breaking: false
---

# 2.5: 更新前端权限测试（permission-driven-ui.test.tsx）

## Description

更新 `frontend/src/__tests__/permission-driven-ui.test.tsx` 中的权限码引用，并新增 `canReadRoles` 守卫测试：

1. 将 `user:read` → `user:list`（用户管理页可见性测试）
2. 将 `user:manage_role` → `role:read`（角色管理页可见性测试）
3. 新增测试：`canReadRoles` 守卫——无 `role:read` 时 `GET /admin/roles` 不发出请求

## Reference Files

- `frontend/src/__tests__/permission-driven-ui.test.tsx` — 现有测试（第 67-90 行）
- `frontend/src/pages/TeamManagementPage.tsx` — 角色下拉守卫实现
- `docs/features/permission-granularity/design/tech-design.md` §Testing Strategy

## Acceptance Criteria

- [ ] 测试"shows user management when user has user:list permission"通过（原 `user:read`）
- [ ] 测试"shows role management when user has role:read permission"通过（原 `user:manage_role`）
- [ ] 新增测试：持有 `role:read` 时，`TeamManagementPage` 发出 `GET /admin/roles` 请求
- [ ] 新增测试：不持有 `role:read` 时，`TeamManagementPage` 不发出 `GET /admin/roles` 请求
- [ ] `user:manage_role` 在测试文件中零残留
- [ ] `npm test` 全部通过

## User Stories

Story 1 AC: 持有 `role:read` → 角色下拉列表正常加载
Story 6 AC: 不持有 `user:list` → 用户管理页入口不显示

## Implementation Notes

- 第 70 行：`teamPermissions: { 1: ['user:read'] }` → `teamPermissions: { 1: ['user:list'] }`
- 第 86 行：`teamPermissions: { 1: ['user:manage_role'] }` → `teamPermissions: { 1: ['role:read'] }`
- 新增 `canReadRoles` 测试：mock `useAuthStore` 的 `hasPermission`，验证 `useQuery` 的 `enabled` 行为
