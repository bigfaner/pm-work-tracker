1、数据库表字段命名未遵循convention，使用了mysql的保留关键字。done
2、累积流式响应的ai message，形成完整的消息体，最后持久化。done，已经支持。
3、根据https://my.feishu.cn/wiki/ODUWwcRCTiPsDGk3TmNcDLgJn1f优化意图识别。doing
4、各个executor agent的system prompt是动态组装的，依赖意图识别的结构化输出，其中要包含操作目标实体的API的Path。非Read操作，由前端根据Path查找API来执行。read 操作无须用户确认，后端根据path查找API
待评估的建议：每一个操作对应一个意图，再加兜底的问答意图。每一个意图都对应一个语义明显且简要的意图编码。LLM的结构化输出中必要包含意图编码。同时，集中管理意图的所有信息。程序可以根据意图编码，读取各个操作所需的信息。
5、数据库表设计是否满足最新的数据建模。done
6、调用卡片的API，要把结果反馈给agent。如果成功，则返回简要的提示信息。如果报非硬性错误（如参数错误），让agent反省并修正。如果报硬性错误（如权限不足），则让agent整理错误信息，返回友好的错误提示。
7、在一个会话中，用户可能中途切换主题（即意图切换），例如先添加待办事项，但是在添加成功前，改为添加子事项。pending。
8、除了执行API的意图之外，必定存在“问答”意图。因为用户不太可能把事情说得清晰，所以agent要主动采访用户，搞清楚用户要做什么。在问答问题中，agent可能会多次调用 read api。当前的planer & executors的设计，似乎是一次只调一个工具，因为read executor没有采用ReAct范式。
9、梳理当前系统，建立专门的知识库，提升问答质量。

待研究：目前一个会话级的state，所有agent都把信息写入到state。这些信息包括AI message、tool message、意图数组等。换句话，各个agent通过state来通信，了解当前会话完成了哪些意图，哪些意图没有完成。根据state来动态构建上下文。

Planner升级为Supervior。Supervior采用ReAct范式，负责跟用户交流，弄清楚用户的意图。如果用户没有说清楚，则主动采访用户。如果用户说清楚了，则执行。

是否把Planner&Executors范式更改为ReAct范式？