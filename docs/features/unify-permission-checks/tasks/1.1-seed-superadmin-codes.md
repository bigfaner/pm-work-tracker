---
id: "1.1"
title: "seedPresetRoles: superadmin role gets all 29 codes"
priority: "P0"
estimated_time: "30min"
dependencies: []
status: pending
breaking: false
---

# 1.1: seedPresetRoles: superadmin role gets all 29 codes

## Description

Update `seedPresetRoles` in `migration/rbac.go` to seed the superadmin role with all 29 permission codes instead of `nil`. The `seedRole` function is additive — on next startup it inserts the 29 codes without affecting existing data.

## Reference Files

- `design/tech-design.md` — Interface 6: seedPresetRoles — Updated
- `backend/internal/migration/rbac.go` — current `seedPresetRoles` function
- `backend/internal/pkg/permissions/codes.go` — `AllCodeStrings()` function

## Acceptance Criteria

- [ ] `seedPresetRoles` calls `seedRole(tx, "superadmin", ..., permissions.AllCodeStrings())` instead of `nil`
- [ ] Backend compiles: `cd backend && go build ./...`
- [ ] Existing tests pass: `cd backend && go test ./...`

## User Stories

- Story 3 (AC3a-c): SuperAdmin has all 29 codes loaded, enabling full operation coverage via permCodes check
- Story 5: `/me/permissions` returns all 29 codes for SuperAdmin (depends on this seed data)

## Implementation Notes

Single-line change in `seedPresetRoles`. The `seedRole` function is additive and idempotent — no migration needed. The superadmin role's `is_preset = true` flag protects it from API modification.
