---
status: "completed"
started: "2026-05-09 01:17"
completed: "2026-05-09 01:24"
time_spent: "~7m"
---

# Task Record: 2.3 Sub-item handler: remove isPMOrSuperAdmin and assignee checks

## Summary
Removed isPMOrSuperAdmin() function and all assignee ownership checks from sub_item_handler.go. Update and ChangeStatus handlers no longer check AssigneeKey != callerBizKey. Added hasPermCode() helper for progress_handler compile dependency. Updated 6 handler tests to reflect new behavior: members/custom roles with sub_item:update or sub_item:change_status can now operate on any sub-item.

## Changes

### Files Created
无

### Files Modified
- backend/internal/handler/sub_item_handler.go
- backend/internal/handler/sub_item_handler_test.go
- backend/internal/handler/progress_handler.go

### Key Decisions
- Added hasPermCode(c, code) helper in sub_item_handler.go to replace isPMOrSuperAdmin() usage in progress_handler.go (compile dependency across tasks 2.3 and 2.4)
- Replaced pmFlag := isPMOrSuperAdmin(c) with skipRegressionCheck := hasPermCode(c, "sub_item:assign") in progress_handler.go so compilation succeeds
- Renamed test functions from assignee-focused names to permission-focused names (e.g., TestUpdateSubItem_Success_AsAssignee -> TestUpdateSubItem_Success_AsMember)
- Tests that previously asserted 403 for non-assignee members now assert 200 since assignee checks are removed

## Test Results
- **Passed**: 348
- **Failed**: 0
- **Coverage**: 83.5%

## Acceptance Criteria
- [x] isPMOrSuperAdmin() function deleted
- [x] No assignee ownership checks (AssigneeKey != callerBizKey) in Update or ChangeStatus handlers
- [x] Backend compiles: cd backend && go build ./...
- [x] Handler tests pass: cd backend && go test ./internal/handler/...

## Notes
Also updated progress_handler.go to replace isPMOrSuperAdmin(c) with hasPermCode(c, "sub_item:assign") since the function was defined in sub_item_handler.go. This is a compile dependency - task 2.4 will do the full progress_handler cleanup including service signature rename.
