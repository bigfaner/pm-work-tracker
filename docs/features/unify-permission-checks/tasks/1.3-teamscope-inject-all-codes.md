---
id: "1.3"
title: "TeamScopeMiddleware: inject all 29 codes for SuperAdmin"
priority: "P0"
estimated_time: "30min"
dependencies: []
status: pending
breaking: false
---

# 1.3: TeamScopeMiddleware: inject all 29 codes for SuperAdmin

## Description

Change `TeamScopeMiddleware` in `middleware/team_scope.go` so that SuperAdmin users receive all 29 permission codes instead of an empty slice. Replace `c.Set("permCodes", []string{})` with `c.Set("permCodes", permissions.AllCodeStrings())`. SuperAdmin still skips the team membership check.

## Reference Files

- `design/tech-design.md` — Interface 2: TeamScopeMiddleware — SuperAdmin Injects All Codes
- `backend/internal/middleware/team_scope.go` — current SuperAdmin branch (lines ~36-41)
- `backend/internal/pkg/permissions/codes.go` — `AllCodeStrings()` function

## Acceptance Criteria

- [ ] SuperAdmin branch sets `permCodes` to `permissions.AllCodeStrings()` instead of `[]string{}`
- [ ] SuperAdmin branch still sets `callerTeamRole` to `"superadmin"`
- [ ] SuperAdmin branch still skips team membership check (`c.Next(); return`)
- [ ] Backend compiles: `cd backend && go build ./...`
- [ ] Existing tests pass: `cd backend && go test ./...`

## User Stories

- Story 3 (AC3a-c): SuperAdmin gets all 29 codes loaded into context, enabling RequirePermission permCodes check to pass
- Story 4: SuperAdmin cross-team access works because TeamScopeMiddleware skips membership check and injects all codes

## Implementation Notes

Single-line change: replace `[]string{}` with `permissions.AllCodeStrings()`. May need to add import for `permissions` package if not already present.
