---
id: "3.2"
title: "Update existing tests (router_test, integration tests)"
priority: "P1"
estimated_time: "2h"
dependencies: ["3.1"]
status: pending
---

# 3.2: Update existing tests (router_test, integration tests)

## Description
Update all test files that construct `config.Config` to use the new nested struct format.

## Reference Files
- `backend/internal/handler/router_test.go`
- `backend/tests/integration/auth_isolation_test.go`
- `backend/tests/integration/progress_completion_test.go`

## Acceptance Criteria
- `router_test.go`: Config construction uses nested struct (e.g., `Auth.JWTSecret`, `CORS.Origins`, `Server.GinMode`)
- `auth_isolation_test.go`: same update
- `progress_completion_test.go`: same update
- All existing tests pass with no behavioral changes
- No test logic changes — only struct construction syntax

## Implementation Notes
- This is a mechanical change — only updating how Config is constructed
- No new test cases needed, just updating field paths
- Run full test suite to verify no regressions
