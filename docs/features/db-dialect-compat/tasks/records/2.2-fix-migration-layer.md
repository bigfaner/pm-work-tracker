---
status: "completed"
started: "2026-04-27 01:17"
completed: "2026-04-27 01:21"
time_spent: "~4m"
---

# Task Record: 2.2 Fix Migration Layer (P3/P4)

## Summary
Fixed migration layer for MySQL compatibility: (P3) extracted rebuildTeamMembersTable DDL into teamMembersDDL/teamMembersDDLMySQL with isMySQL branching, using BIGINT UNSIGNED AUTO_INCREMENT + PRIMARY KEY(id) + separate CREATE UNIQUE INDEX for MySQL; (P4) made HasColumn delegate to columnExists which already has isMySQL branching via information_schema.

## Changes

### Files Created
无

### Files Modified
- backend/internal/migration/rbac.go
- backend/internal/migration/rbac_test.go

### Key Decisions
- 2.2: Extracted teamMembersDDL(db) function following existing rbacTableDDL pattern, with teamMembersDDLMySQL() as a separate exported-for-testing helper
- 2.2: MySQL DDL uses BIGINT UNSIGNED NOT NULL AUTO_INCREMENT + PRIMARY KEY(id) + separate CREATE UNIQUE INDEX (MySQL doesn't support inline UNIQUE on column definitions in all contexts)
- 2.2: HasColumn now delegates to columnExists instead of using hardcoded pragma_table_info, reusing existing isMySQL branching

## Test Results
- **Passed**: 26
- **Failed**: 0
- **Coverage**: 76.6%

## Acceptance Criteria
- [x] rebuildTeamMembersTable has isMySQL branch for DDL: uses AUTO_INCREMENT + PRIMARY KEY(id) on MySQL, AUTOINCREMENT on SQLite
- [x] rebuildTeamMembersTable datetime('now') default uses isMySQL branch for CURRENT_TIMESTAMP on MySQL
- [x] HasColumn delegates to columnExists (or implements equivalent isMySQL branch)
- [x] Existing migration tests pass: go test ./internal/migration/ -count=1
- [x] Project builds: go build ./cmd/server/

## Notes
无
