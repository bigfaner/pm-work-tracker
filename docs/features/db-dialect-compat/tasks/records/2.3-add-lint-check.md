---
status: "completed"
started: "2026-04-27 01:23"
completed: "2026-04-27 01:28"
time_spent: "~5m"
---

# Task Record: 2.3 Add SQLite Keyword Lint Check

## Summary
Added check_sqlite_keywords function to lint-staged.sh that detects hardcoded SQLite-only SQL syntax (SUBSTR, CAST AS INTEGER, datetime now, pragma_table_info) in the repository layer, blocking commits with clear error messages. Code using dialect.CastInt()/dialect.Substr()/dialect.Now() passes the check. Supports nosqlite inline marker for legitimate exceptions.

## Changes

### Files Created
无

### Files Modified
- scripts/lint-staged.sh

### Key Decisions
- Escaped parentheses in datetime pattern: datetime\(.*now.*\) instead of literal datetime(.now.) for correct extended regex matching
- Changed nosqlite filter from '# nosqlite' to just 'nosqlite' to support both Go (// nosqlite) and shell (# nosqlite) comment styles
- Function placed after TS/TSX check section, called from main lint flow before 'lint-staged done' banner

## Test Results
- **Passed**: 7
- **Failed**: 0
- **Coverage**: N/A (task has no tests)

## Acceptance Criteria
- [x] check_sqlite_keywords function added to lint-staged.sh
- [x] Detects patterns: SUBSTR(, CAST(...AS INTEGER), datetime(.now.), pragma_table_info
- [x] Scan scope: backend/internal/repository/gorm/*.go only (excluding *_test.go)
- [x] Hardcoded SQLite SQL in repo layer => commit blocked with clear error message
- [x] Code using dialect.CastInt() / dialect.Substr() / dialect.Now() => commit passes
- [x] Function is called from the main lint flow

## Notes
无
