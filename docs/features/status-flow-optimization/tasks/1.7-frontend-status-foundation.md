---
id: "1.7"
title: "Frontend status foundation"
priority: "P1"
estimated_time: "2-3h"
dependencies: ["1.4", "1.5"]
status: pending
---

# 1.7: Frontend Status Foundation

## Description

Create frontend status constants, update TypeScript types, update StatusBadge to use code-based lookup, and replace all hardcoded Chinese STATUS_OPTIONS with English codes.

## Reference Files

- Design: "Frontend Changes" section (status.ts, StatusBadge, types)
- New: `frontend/src/lib/status.ts`
- Modified: `frontend/src/types/index.ts`
- Modified: `frontend/src/components/shared/StatusBadge.tsx`
- Modified: `frontend/src/pages/ItemViewPage.tsx`
- Modified: `frontend/src/pages/MainItemDetailPage.tsx`
- Modified: `frontend/src/pages/TableViewPage.tsx`
- Modified: `frontend/src/mocks/handlers.ts`
- Modified: `frontend/src/components/shared/StatusBadge.test.tsx`

## Acceptance Criteria

- [ ] `lib/status.ts` exports `MAIN_ITEM_STATUSES` (7 entries) and `SUB_ITEM_STATUSES` (6 entries) with name, variant, terminal fields (AC-1 partial)
- [ ] `lib/status.ts` exports `isOverdue(expectedEndDate, status)` function
- [ ] `UpdateMainItemReq` no longer has `status` field
- [ ] `MainItem` and `SubItem` interfaces include `statusName?: string`
- [ ] StatusBadge uses code-based variant lookup from `lib/status.ts` instead of Chinese string map (AC-18)
- [ ] StatusBadge displays `statusName` from API response (falls back to map)
- [ ] `STATUS_OPTIONS` arrays replaced with English codes in all 3 pages
- [ ] Mock handlers updated to return English status codes
- [ ] StatusBadge tests updated: render with code, verify Chinese name display, verify variant class
- [ ] `lib/status.ts` tests: isOverdue returns correct values
- [ ] All frontend tests pass

## Implementation Notes

- StatusBadge: `const def = MAIN_ITEM_STATUSES[status] || SUB_ITEM_STATUSES[status]`
- Display text: prefer `statusName` prop, fallback to `def?.name`
- Variant class: use `def?.variant` mapped to existing CSS class pattern
- STATUS_OPTIONS change: `['未开始', ...]` → `['pending', ...]` — but these are now display-only since StatusDropdown will use available-transitions API (task 3.1)
