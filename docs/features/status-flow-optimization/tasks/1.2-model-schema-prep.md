---
id: "1.2"
title: "Model, schema & DTO preparatory changes"
priority: "P0"
estimated_time: "1.5h"
dependencies: ["1.1"]
status: pending
---

# 1.2: Model, Schema & DTO Preparatory Changes

## Description

Make all coordinated model/schema/DTO changes needed before implementing the state machine. Remove DelayCount, update default status values, create StatusHistory model, update DTOs, and add permission code.

## Reference Files

- Design: "Model Changes", "Modified DTO", "New Permission Code" sections
- Modified: `backend/internal/model/main_item.go`
- Modified: `backend/internal/model/sub_item.go`
- New: `backend/internal/model/status_history.go`
- Modified: `backend/internal/migration/runner.go`
- Modified: `backend/internal/dto/item_dto.go`
- Modified: `backend/internal/pkg/permissions/codes.go`
- Modified: `backend/internal/service/main_item_service.go` (Update, Archive, Create methods only)
- Modified: `backend/internal/service/sub_item_service.go` (Create method only)

## Acceptance Criteria

- [ ] `MainItem.DelayCount` field removed
- [ ] `SubItem.DelayCount` field removed
- [ ] MainItem default status changed to `"pending"`
- [ ] SubItem default status changed to `"pending"`
- [ ] SubItem 8-status comment block removed
- [ ] `StatusHistory` model created (does NOT embed BaseModel)
- [ ] `StatusHistory` registered in `migration/runner.go`
- [ ] `MainItemUpdateReq.Status *string` field removed from DTO
- [ ] `main_item:change_status` permission added to codes.go
- [ ] `MainItemService.Update` no longer handles status field
- [ ] `MainItemService.Archive` uses `"completed"` / `"closed"` instead of Chinese
- [ ] `MainItemService.Create` default status is `"pending"`
- [ ] `SubItemService.Create` default status is `"pending"`
- [ ] All existing tests pass after these changes (may need string literal updates)

## Implementation Notes

- StatusHistory model: no soft delete, no UpdatedAt — append-only log
- Index `idx_item` on (item_type, item_id) for query performance
- When removing Status from MainItemUpdateReq, also update any test that sets it
- The Archive string literal changes are straightforward find-replace
