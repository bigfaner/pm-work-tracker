---
id: "1.3"
title: "StatusHistory repo & service"
priority: "P0"
estimated_time: "2h"
dependencies: ["1.2"]
status: pending
---

# 1.3: StatusHistory Repo & Service

## Description

Create the StatusHistory repository (interface + GORM implementation) and service layer for recording status change audit logs.

## Reference Files

- Design: "New Repository: StatusHistoryRepo", "New Service: StatusHistoryService" sections
- New: `backend/internal/repository/status_history_repo.go`
- New: `backend/internal/repository/gorm/status_history_repo.go`
- New: `backend/internal/service/status_history_service.go`
- New: `backend/internal/repository/status_history_repo_test.go`
- New: `backend/internal/repository/gorm/status_history_repo_test.go`
- New: `backend/internal/service/status_history_service_test.go`

## Acceptance Criteria

- [ ] `StatusHistoryRepo` interface defines `Create` and `ListByItem` methods with correct signatures
- [ ] GORM impl follows constructor pattern: `NewGormStatusHistoryRepo(db) StatusHistoryRepo`
- [ ] Constructor validates non-nil db, panics on nil
- [ ] `StatusHistoryService` interface defines `Record` and `ListByItem`
- [ ] Service constructor takes `StatusHistoryRepo`, validates non-nil with panic
- [ ] `ListByItem` supports pagination via `dto.Pagination`
- [ ] `FindByID` in GORM impl translates `gorm.ErrRecordNotFound` to `errors.ErrNotFound`
- [ ] Unit tests for service with mock repo pass
- [ ] ≥90% coverage for new code

## Implementation Notes

- Follow existing patterns exactly: see `repository/sub_item_repo.go` for interface, `repository/gorm/sub_item_repo.go` for impl
- Service is a thin wrapper — no business logic beyond delegating to repo
- Repo test: use real GORM with SQLite in-memory (project pattern)
