---
id: "11.9"
title: "Frontend Integration Tests: Key Page Flows with MSW Mocks"
priority: "P2"
estimated_time: "3h"
dependencies: "9.1, 9.4, 11.8"
status: pending
---

# 11.9: Frontend Integration Tests: Key Page Flows with MSW Mocks

## Description
Write page-level integration tests using MSW (Mock Service Worker) to mock API responses and verify key user flows end-to-end in the browser-like test environment.

## Reference Files
- `design/tech-design.md` — Testing Strategy / Frontend Tests (MSW, no E2E in v1)
- `frontend/src/pages/` (tasks 9.1, 9.4, 8.1)

## Acceptance Criteria
- MSW setup: `frontend/src/mocks/handlers.ts` defines handlers for all API endpoints used in tests; `frontend/src/mocks/server.ts` exports a `setupServer` instance
- Login flow test: render `LoginPage`, fill credentials, submit → MSW returns 200 → user redirected to `/items`
- Login failure test: MSW returns 401 → inline error displayed, no redirect
- 事项视图 page test: MSW returns 3 main items → all 3 rendered in Collapse; filter by P1 → only P1 items shown (re-fetch with filter param)
- 事项池 page test: MSW returns mix of 待分配/已分配/已拒绝 items → correct card styles; click "分配" → assign modal opens; submit assign → MSW returns 200 → card updates to 已分配
- Auth store cleared on 401: any API call returning 401 → `clearAuth()` called → redirect to `/login`
- All tests run with `vitest --run` in < 60 seconds

## Implementation Notes
- Use `msw/node` for Vitest (not the browser service worker)
- `beforeAll(server.listen)`, `afterEach(server.resetHandlers)`, `afterAll(server.close)` in test setup file
- Configure Vitest `setupFiles` to include the MSW server setup
- Test file location: `frontend/src/__tests__/` or co-located with pages as `*.integration.test.tsx`
- Coverage target for frontend components: ≥ 60%
