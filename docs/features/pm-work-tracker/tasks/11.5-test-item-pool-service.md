---
id: "11.5"
title: "Backend Unit Tests: ItemPoolService"
priority: "P1"
estimated_time: "2h"
dependencies: "4.5"
status: pending
---

# 11.5: Backend Unit Tests: ItemPoolService

## Description
Write unit tests for `ItemPoolService.Assign` focusing on the atomic transaction behavior and rollback on failure.

## Reference Files
- `design/tech-design.md` — Testing Strategy (ItemPoolService.Assign transaction rollback)
- `design/tech-design.md` — Key Constraints Summary (ItemPool assign → creates SubItem atomically)
- `internal/service/item_pool_service.go` (task 4.5)

## Acceptance Criteria
- `Assign` with valid pool item (status=待分配) → succeeds, returns new SubItemID
- `Assign` with pool item status=已分配 → returns `ITEM_ALREADY_PROCESSED` error
- `Assign` with pool item status=已拒绝 → returns `ITEM_ALREADY_PROCESSED` error
- `Assign` when SubItem creation fails → transaction rolls back; `ItemPool.Status` remains `待分配`
- `Reject` with valid pool item → succeeds, status=已拒绝, reason stored
- `Reject` with already-processed item → returns `ITEM_ALREADY_PROCESSED`
- `Submit` → creates pool item with status=待分配
- Coverage for `ItemPoolService` ≥ 80%

## Implementation Notes
- Transaction rollback test: mock `SubItemRepo.Create` to return an error; assert that `ItemPoolRepo.Update` was NOT committed (or was rolled back)
- Use `testify/mock` for all repos
- The transaction test requires careful mock setup — consider using a real in-memory SQLite DB for the transaction test specifically, rather than mocks
