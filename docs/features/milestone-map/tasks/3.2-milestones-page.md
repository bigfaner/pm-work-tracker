---
id: "3.2"
title: "Build: MilestonesPage (UF-1 Two-Level View + UF-7 Map Dialog)"
priority: "P0"
estimated_time: "4h"
dependencies: ["3.1"]
status: pending
breaking: false
noTest: false
mainSession: false
---

# 3.2: Build: MilestonesPage (UF-1 Two-Level View + UF-7 Map Dialog)

## Description

Build the new /milestones page with two-level view: map card list (first level) → click into timeline view (second level). Includes create/edit milestone map dialog (UF-7), status filter, breadcrumb navigation, zoom controls, and all states (loading/empty/error/populated).

## Reference Files

- `ui/ui-design.md` — Component "里程碑图列表视图" (UF-1 first level) + "里程碑时间线视图" (UF-1 second level)
- `ui/prototype/milestones.html` — Full interactive prototype with both views
- `ui/prototype/styles.css` — Shared CSS patterns
- `ui/prototype/app.js` — Interaction patterns
- `design/tech-design.md` — Interfaces, Cross-Layer Data Map
- `design/api-handbook.md` — MilestoneMap endpoints

## Acceptance Criteria

- [ ] Route `/milestones` registered in App.tsx with ProtectedRoute + AppLayout
- [ ] Navigation sidebar updated: add "里程碑图" link between "事项清单" and "甘特图"
- [ ] List view: card grid showing all maps with name, status badge, milestone count, item count, progress bar, mini timeline thumbnail
- [ ] List view: status filter dropdown (全部/规划中/已评审/待实施/实施中/已完成)
- [ ] List view: "+ 创建里程碑图" button + refresh button
- [ ] List view: create map dialog (UF-7) with mapName + mapDesc fields
- [ ] Timeline view: breadcrumb nav back to list, milestone nodes with arrows, MI items
- [ ] Timeline view: zoom controls (周/月/季)
- [ ] States: loading (skeleton), empty ("暂无里程碑图" + create button), error ("加载失败，请重试" + retry)
- [ ] Permission-aware: create button disabled when no `milestone:create` permission
- [ ] Click card → enters timeline view; click milestone → triggers detail panel (handled by 3.3)
- [ ] `npm test` passes for new components

## User Stories

- Story 1: PM 创建里程碑图 (via UF-7 dialog)
- Story 2: PM 编辑里程碑图信息 (via UF-7 edit mode)
- Story 3: PM 切换里程碑图状态 (status badge on card)
- Story 8: 里程碑图两级视图 (list + timeline)
- Story 8: 空状态提示 (0 个里程碑图)
- Story 8: 时间线缩放 (周/月/季)
- Story 8: 加载失败错误状态
- Story 9: 空状态+无权限
- Story 9: 禁用操作按钮+tooltip

## Implementation Notes

- Page assembly task: creates page file, registers route, composes all components
- Permission check uses existing `usePermissionStore` or equivalent
- Status badge styling matches existing items list pattern (Badge + dropdown)
