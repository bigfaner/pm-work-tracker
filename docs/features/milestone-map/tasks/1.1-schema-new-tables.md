---
id: "1.1"
title: "Schema: create pmw_milestone_maps and pmw_milestones tables"
priority: "P0"
estimated_time: "1-2h"
complexity: "medium"
dependencies: []
surface-key: "backend"
surface-type: "api"
breaking: false
type: "coding.feature"
mainSession: false
---

# 1.1: Schema: create pmw_milestone_maps and pmw_milestones tables

## Description
Create DDL for two new tables (pmw_milestone_maps, pmw_milestones) in both MySQL 8.0 and SQLite dialects, as defined in the ER diagram and schema.sql design artifacts.

## Reference Files
- docs/features/milestone-map/design/tech-design.md — Data Models, Architecture Layer Placement
- docs/features/milestone-map/design/er-diagram.md — Entity Details, Index Design
- docs/features/milestone-map/design/schema.sql — Full DDL source (both dialects)

## Acceptance Criteria
- [ ] MySQL DDL for `pmw_milestone_maps` and `pmw_milestones` executes without error
- [ ] SQLite DDL for both tables executes without error
- [ ] All indexes created: `uk_biz_key`, `uk_*_team_name_deleted` (team-scoped unique), `idx_*_team_status`
- [ ] Unique constraint `uk_*_team_name_deleted` enforces team-scoped name uniqueness with soft-delete support

## User Stories
No direct user story mapping — infrastructure task.

## Hard Rules
- DDL must be applied to BOTH `backend/migrations/SQLite-schema.sql` AND `backend/migrations/MySql-schema.sql`
- No DDL FOREIGN KEY constraints (project convention)

## Implementation Notes
### Affected Files
| File | Action |
|------|--------|
| backend/migrations/SQLite-schema.sql | Append CREATE TABLE + CREATE INDEX |
| backend/migrations/MySql-schema.sql | Append CREATE TABLE + CREATE INDEX |

### Test Impact
- Affected test suite(s): `backend/internal/model/` (model tests validate table names)
- Expected fixture changes: none (new tables, no existing fixtures)
- Risk level: low
