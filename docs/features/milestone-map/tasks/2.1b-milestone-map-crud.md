---
id: "2.1b"
title: "MilestoneMap Basic CRUD: Create + List + Get + Update"
priority: "P0"
estimated_time: "1h"
dependencies: ["2.1a"]
status: pending
breaking: false
noTest: false
mainSession: false
scope: backend
---

# 2.1b: MilestoneMap Basic CRUD

## Description

Implement the 4 simple CRUD endpoints for MilestoneMap: Create, List (paginated with computed fields), Get, Update.

## Reference Files

- `design/api-handbook.md` — Create, List, Get, Update endpoints
- `backend/internal/service/main_item_service.go` — Service pattern (bizKey boundary, snowflake generation)
- `backend/internal/handler/main_item_handler.go` — Handler pattern (BizKey param parsing, VO conversion)

## Acceptance Criteria

- [ ] `service/milestone_map_service.go`: Interface + implementation with Create, Get, GetByBizKey, List, Update
- [ ] `handler/milestone_map_handler.go`: Create, List, Get, Update handlers
- [ ] Create: name validation (1-100 chars), default status `planning`, snowflake bizKey generation
- [ ] List: paginated, status filter, computed fields (milestoneCount=0, itemCount=0, overallProgress=0 — stubs for now)
- [ ] Get: by bizKey, 404 if not found
- [ ] Update: partial update (only passed fields), name validation
- [ ] Routes registered: POST list, GET list, GET detail, PUT update
- [ ] `just test` passes

## Implementation Notes

- List computed fields: stub with zero values for now. Real computation will be in 2.1c after milestones exist.
- Follow main_item_service.go patterns for bizKey generation and team-scoped queries.
- Handler: parse `:mapId` as bizKey from URL, use dto for request/response binding.
