---
id: "1.1"
title: "Schema Migration: milestone_maps + milestones + main_items ALTER"
priority: "P0"
estimated_time: "1h"
dependencies: []
status: pending
breaking: true
noTest: false
mainSession: false
---

# 1.1: Schema Migration: milestone_maps + milestones + main_items ALTER

## Description

Create database migration for two new tables (`pmw_milestone_maps`, `pmw_milestones`) and add `milestone_key` column to `pmw_main_items`. Update both SQLite and MySQL schema files to stay in sync.

## Reference Files

- `design/er-diagram.md` — Entity relationships, column details, index design
- `design/schema.sql` — Full DDL for both dialects
- `backend/migrations/SQLite-schema.sql` — Existing SQLite schema (append new tables + ALTER)
- `backend/migrations/MySql-schema.sql` — Existing MySQL schema (append new tables + ALTER)

## Acceptance Criteria

- [ ] `pmw_milestone_maps` table created with all columns matching `design/schema.sql`
- [ ] `pmw_milestones` table created with all columns matching `design/schema.sql`
- [ ] `pmw_main_items` ALTER adds `milestone_key BIGINT DEFAULT NULL` column
- [ ] All indexes from `design/schema.sql` created (unique biz_key, team+status, milestone_map_key, etc.)
- [ ] SQLite and MySQL schemas stay in sync (both updated)
- [ ] `just compile` passes (GORM auto-migration compatible)

## User Stories

- Story 4c: "删除里程碑 → 所有关联 MI 的 milestone_key 在同一事务内置空" (requires milestone_key column)

## Implementation Notes

- Follow existing schema patterns exactly (BaseModel fields, COMMENT syntax, index naming)
- `milestone_key` on `pmw_main_items` has no DDL FK constraint (DM-003), only an index
- `map_status` and `milestone_status` follow DM-004 (entity-prefixed, not bare `status`)
- `map_name`/`map_desc`/`milestone_name` follow entity-prefix pattern (not bare `name`/`description`)
