---
id: "1.3"
title: "MilestoneMap and Milestone data models + MainItem modification"
priority: "P0"
estimated_time: "2-3h"
complexity: "high"
dependencies: ["1.1", "1.2"]
surface-key: "backend"
surface-type: "api"
breaking: false
type: "coding.feature"
mainSession: false
---

# 1.3: MilestoneMap and Milestone data models + MainItem modification

## Description
Create Go model structs for MilestoneMap and Milestone, matching the schema design. Add `MilestoneKey` field to the existing MainItem model. All models embed BaseModel for common fields (ID, BizKey, TeamKey, etc.).

## Reference Files
- docs/features/milestone-map/design/tech-design.md — Data Models (MilestoneMap Model, Milestone Model, MainItem Model Modified), Field Quick Reference
- docs/features/milestone-map/design/schema.sql — Column types and constraints
- docs/features/milestone-map/design/er-diagram.md — Entity Details

## Acceptance Criteria
- [ ] `MilestoneMap` struct in `model/milestone_map.go` compiles with all fields matching schema (TeamKey, CreatorKey, AssigneeKey, MapName, MapDesc, MapStatus, PlanStartDate, ExpectedEndDate)
- [ ] `Milestone` struct in `model/milestone.go` compiles with all fields matching schema (TeamKey, MilestoneMapKey, MilestoneName, MilestoneDesc, ExpectedEndDate, MilestoneStatus)
- [ ] `MainItem` struct in `model/main_item.go` updated with `MilestoneKey *int64` field (`gorm:"index"`)
- [ ] `TableName()` methods return correct table names: `pmw_milestone_maps`, `pmw_milestones`
- [ ] Models compile without error alongside existing models

## User Stories
No direct user story mapping — infrastructure task.

## Implementation Notes
### Affected Files
| File | Action |
|------|--------|
| backend/internal/model/milestone_map.go | Create |
| backend/internal/model/milestone.go | Create |
| backend/internal/model/main_item.go | Modify (add MilestoneKey field) |

### Test Impact
- Affected test suite(s): `backend/internal/model/`
- Expected fixture changes: none (model-level, no fixture impact)
- Risk level: low
