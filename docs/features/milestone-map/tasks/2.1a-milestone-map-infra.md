---
id: "2.1a"
title: "MilestoneMap Infrastructure: Repo + GORM + Helpers + Routes Skeleton"
priority: "P0"
estimated_time: "1h"
dependencies: ["1.gate"]
status: pending
breaking: false
noTest: false
mainSession: false
scope: backend
---

# 2.1a: MilestoneMap Infrastructure

## Description

Set up the foundation for MilestoneMap: repository interface, GORM implementation, helper extensions (error codes, repo helpers), and route registration skeleton. No business logic yet — just the plumbing.

## Reference Files

- `design/tech-design.md` — MilestoneMapRepo interface
- `backend/internal/repository/main_item_repo.go` — Repo interface pattern
- `backend/internal/repository/gorm/main_item_repo.go` — GORM implementation pattern
- `backend/internal/pkg/repo/helpers.go` — identifiable interface, acceptedFields, getID switch
- `backend/internal/pkg/errors/errors.go` — Error code pattern
- `backend/internal/handler/router.go` — Route registration pattern

## Acceptance Criteria

- [ ] `repository/milestone_map_repo.go`: Interface with Create, FindByID, FindByBizKey, Update, List (paginated), SoftDelete
- [ ] `repository/gorm/milestone_map_repo.go`: GORM implementation with team-scoped queries, status filter
- [ ] `errors.go`: MilestoneMap error codes added (ErrMilestoneMapNotFound)
- [ ] `repo/helpers.go`: MilestoneMap added to identifiable interface, acceptedFields, getID switch
- [ ] `handler/router.go`: Placeholder route group for milestone-maps under teams
- [ ] `just test` passes

## Implementation Notes

- Follow exact patterns from main_item_repo.go for the interface
- GORM repo: inject `*gorm.DB`, use common.go scopes for team-scoping
- acceptedFields: add `model.MilestoneMap{}.TableName()` → {map_name, map_desc, map_status} fields
- Route skeleton: just register the group, no handler wiring yet
