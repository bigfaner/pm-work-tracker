---
id: "2.9"
title: "Router wiring and dependency injection"
priority: "P1"
estimated_time: "1-2h"
complexity: "medium"
dependencies: ["2.7", "2.8", "1.gate"]
surface-key: "backend"
surface-type: "api"
breaking: false
type: "coding.feature"
mainSession: false
---

# 2.9: Router wiring and dependency injection

## Description
Update the router Dependencies struct and SetupRouter to register all 14 milestone routes with correct permission middleware. Wire handler constructors with repo and service dependencies.

## Reference Files
- docs/features/milestone-map/design/tech-design.md — Router Wiring (Dependencies Struct MODIFIED, Route Registration MODIFIED)
- backend/internal/handler/router.go — existing router setup

## Acceptance Criteria
- [ ] `Dependencies` struct updated with `MilestoneMap *MilestoneMapHandler` and `Milestone *MilestoneHandler` fields
- [ ] All 7 MilestoneMap routes registered with correct permission middleware (`milestone:create/read/update/delete`)
- [ ] All 7 Milestone routes registered (nested + direct access) with correct permission middleware
- [ ] Route parameters `:mapId` and `:milestoneId` resolve correctly via `ParseBizKeyParam`

## User Stories
No direct user story mapping — wiring task enabling all Stories 1-8.

## Implementation Notes
### Affected Files
| File | Action |
|------|--------|
| backend/internal/handler/router.go | Modify (add Dependencies fields + route registration) |

### Test Impact
- Affected test suite(s): integration/API tests
- Expected fixture changes: none
- Risk level: low — additive routes, no existing routes changed
