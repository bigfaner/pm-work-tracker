---
status: "completed"
started: "2026-06-07 14:40"
completed: "2026-06-07 14:56"
time_spent: "~16m"
---

# Task Record: 1.2 Schema: ALTER pmw_main_items add milestone_key column

## Summary
Added milestone_key (nullable BIGINT/INTEGER) column to pmw_main_items table in both MySQL and SQLite schemas, with B-tree index idx_main_items_milestone_key. Added MilestoneKey *int64 field to MainItem Go model with GORM index tag. No DDL foreign key constraint per project convention.

## Changes

### Files Created
无

### Files Modified
- backend/migrations/MySql-schema.sql
- backend/migrations/SQLite-schema.sql
- backend/internal/model/main_item.go
- backend/cmd/server/main_test.go

### Key Decisions
- Used explicit index name idx_main_items_milestone_key in GORM tag to match DDL naming convention (DM-012)
- Placed milestone_key column after sub_item_seq in DDL for logical grouping
- No new tests needed -- column is nullable, existing fixtures remain valid
- Fixed pre-existing TestRun_FailsWhenAssetsInvalid test -- assertion expected 'migration error:' but actual flow hits asset validation first

## Test Results
- **Tests Executed**: Yes
- **Passed**: 17
- **Failed**: 0
- **Coverage**: 100.0%

## Acceptance Criteria
- [x] MySQL ALTER adds milestone_key BIGINT nullable column to pmw_main_items
- [x] SQLite ALTER adds milestone_key INTEGER column to pmw_main_items
- [x] Index idx_main_items_milestone_key created on milestone_key
- [x] Existing data unaffected (column nullable, default NULL, backward compatible)

## Notes
Schema-only change. All 17 existing MainItem repo tests pass. All 42 model tests pass. Compile, fmt, lint all clean.
