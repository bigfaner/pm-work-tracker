---
status: "completed"
started: "2026-05-12 20:12"
completed: "2026-05-12 20:19"
time_spent: "~7m"
---

# Task Record: 1.1 Schema Migration: milestone_maps + milestones + main_items ALTER

## Summary
Created database migration for pmw_milestone_maps and pmw_milestones tables, added milestone_key column to pmw_main_items. Created Go model structs (MilestoneMap, Milestone) with GORM tags matching design schema. Updated both SQLite and MySQL schema DDL files with new tables, indexes, and ALTER TABLE statements.

## Changes

### Files Created
- backend/internal/model/milestone.go
- backend/internal/model/milestone_test.go

### Files Modified
- backend/internal/model/main_item.go
- backend/migrations/SQLite-schema.sql
- backend/migrations/MySql-schema.sql

### Key Decisions
- MilestoneMap and Milestone models embed BaseModel following DM-002 convention
- Status fields use entity-prefixed naming (map_status, milestone_status) per DM-004
- milestone_key on MainItem is *int64 (nullable pointer) since NULL means unassigned, per DM-003 no DDL FK constraint
- BizKey uniqueness enforced at DDL level via uk_*_biz_key indexes, not GORM AutoMigrate tags (consistent with existing models)

## Test Results
- **Tests Executed**: Yes
- **Passed**: 8
- **Failed**: 0
- **Coverage**: 100.0%

## Acceptance Criteria
- [x] pmw_milestone_maps table created with all columns matching design/schema.sql
- [x] pmw_milestones table created with all columns matching design/schema.sql
- [x] pmw_main_items ALTER adds milestone_key BIGINT DEFAULT NULL column
- [x] All indexes from design/schema.sql created (unique biz_key, team+status, milestone_map_key, etc.)
- [x] SQLite and MySQL schemas stay in sync (both updated)
- [x] just compile passes (GORM auto-migration compatible)

## Notes
8 new tests: 3 for MilestoneMap (TableName, Defaults, DescNullable), 3 for Milestone (TableName, Defaults, ExpectedEndDateSet), 2 for MainItem.MilestoneKey (set and null). Full backend test suite (24 packages) all pass. Pre-existing lint issues in 3 unrelated files (filter_helpers.go, item_pool_repo.go, item_pool_vo.go) are not caused by this change.
