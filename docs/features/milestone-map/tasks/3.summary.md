---
id: "3.summary"
title: "Phase 3 Summary"
priority: "P0"
estimated_time: "15min"
dependencies: ["3.1", "3.2", "3.3", "3.4", "3.5", "3.6", "3.7"]
type: "doc.summary"
mainSession: false
---

# 3.summary: Phase 3 Summary

## Description

Generate a structured summary of all completed tasks in phase 3. This summary is read by subsequent phase tasks to maintain cross-phase consistency.

## Instructions

### Step 1: Read all phase task records

Read each record file from `docs/features/milestone-map/tasks/records/` whose filename starts with `3.` and does NOT contain `.summary` (e.g., `3.1-*.md`, `3.2-*.md`). Exclude the phase summary's own prior record if one exists.

### Step 2: Extract structured data into the summary field

The `summary` field in `record.json` MUST follow this exact template. Copy it verbatim and fill in the values.

## Reference Files

- All phase task records: `docs/features/milestone-map/tasks/records/3.*.md`
- Design reference: `docs/features/milestone-map/design/tech-design.md`

## Acceptance Criteria

- [ ] All phase task records have been read
- [ ] Summary follows the exact 5-section template
- [ ] Types & Interfaces Changed table is populated
- [ ] Record created via `/submit-task`

## Hard Rules

- MUST NOT write new feature code — this is documentation only
